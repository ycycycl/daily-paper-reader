---
title: "Attend Locally, Remember Linearly: Linear Attention as Cross-Frame Memory for Autoregressive Video Diffusion"
title_zh: 局部关注，线性记忆：将线性注意力作为自回归视频扩散的跨帧记忆
authors: "Kunyang Li, Mubarak Shah, Yuzhang Shang"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.16579v1"
tags: ["query:upstream-ad-planning-idea"]
score: 8.5
evidence: 流式视频扩散的跨帧记忆
tldr: 本文面向自回归视频扩散在长视频生成中因 softmax 注意力和 KV 缓存带来的二次计算与线性内存增长问题，提出 ARL2 混合注意力模块：用帧内 softmax 保留空间细节，用帧间门控循环线性状态承载跨帧记忆，从而实现线性时间和常数内存，并提升长程时序一致性。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.16579v1/fig-001.webp\", \"caption\": \"Figure 4: (a) Per-dimension ARR averaged across layers. Four Hybrid-Recoverable (HR) dimensions achieve ≥85% recovery, while ImgQual is Hybrid-Sensitive (HS) at ∼25%, motivating HS-aware layer selection to minimize quality degradation. (b) Each point corresponds to one of the 30 layers, plotted by ∆HR (x-axis) and ∆HS (y-axis). Here the 50% lowest-pℓ layers (blue) are selected for replacement. (c) End-to-end validation after Stage 2: the hybrid model matches or exceeds the baseline on all HR dimensions and recovers ImgQual to within ∼1%, indicating that HS-aware selection preserves sensitive dimensions while allowing HR gaps to be closed via joint distillation.\", \"page\": 6, \"index\": 1, \"width\": 825, \"height\": 243, \"caption_zh\": \"图 4：（a）跨层平均的逐维度 ARR。四个 Hybrid-Recoverable（HR）维度达到 ≥85% 的恢复率，而 ImgQual 为 Hybrid-Sensitive（HS），约为 ∼25%，这促使采用 HS 感知的层选择以最小化质量退化。（b）每个点对应 30 层中的一层，按 ∆HR（x 轴）和 ∆HS（y 轴）绘制。此处选择 50% 最低-pℓ 层（蓝色）进行替换。（c）Stage 2 后的端到端验证：混合模型在所有 HR 维度上达到或超过基线，并将 ImgQual 恢复到 ∼1% 以内，表明 HS 感知选择在允许通过联合蒸馏弥合 HR 差距的同时，保留了敏感维度。\"}, {\"url\": \"assets/figures/arxiv/2605.16579v1/fig-002.webp\", \"caption\": \"Table 5: Stress-test prompts for sensitivity-guided layer selection. Each prompt targets a specific state weakness axis: TC = temporal consistency, DY = motion dynamics, SQ = spatial quality.\", \"page\": 15, \"index\": 2, \"width\": 830, \"height\": 303, \"caption_zh\": \"表 5：用于敏感性引导层选择的压力测试提示。每个提示针对一个特定的状态弱点轴：TC = 时间一致性，DY = 运动动态，SQ = 空间质量。\"}, {\"url\": \"assets/figures/arxiv/2605.16579v1/fig-003.webp\", \"caption\": \"Table 6: Detailed VBench per-dimension scores. ‡: native resolution 544×960, 97 frames.\", \"page\": 15, \"index\": 3, \"width\": 830, \"height\": 521, \"caption_zh\": \"表 6：详细的 VBench 逐维度得分。‡：原生分辨率 544×960，97 帧。\"}, {\"url\": \"assets/figures/arxiv/2605.16579v1/fig-004.webp\", \"caption\": \"Figure 1: (a) AR video diffusion relies on softmax self-attention with a growing KV cache, incurring O(N) memory, which limits scalability. (b) Replacing self-attention with our hybrid linear attention, the model maintains a fixed-size recurrent state for streaming context with O(1) memory.\", \"page\": 2, \"index\": 4, \"width\": 824, \"height\": 237, \"caption_zh\": \"图 1：（a）AR 视频扩散依赖于带有不断增长的 KV cache 的 softmax self-attention，产生 O(N) 内存开销，从而限制了可扩展性。（b）将 self-attention 替换为我们的 hybrid linear attention 后，模型为流式上下文维持固定大小的循环状态，具有 O(1) 内存开销。\"}, {\"url\": \"assets/figures/arxiv/2605.16579v1/fig-005.webp\", \"caption\": \"Table 1: ARL2 achieves comparable overall performance to representative open-source video models, while improving temporal flickering and background consistency, with O(1) memory complexity. ARL2 (50%/75%) denotes replacing 50%/75% of attention layers with hybrid linear attention.\", \"page\": 7, \"index\": 5, \"width\": 830, \"height\": 436, \"caption_zh\": \"表 1：ARL2 在总体性能上达到与代表性开源视频模型相当的水平，同时改善了时间闪烁和背景一致性，并具有 O(1) 内存复杂度。ARL2（50%/75%）表示用 hybrid linear attention 替换 50%/75% 的注意力层。\"}, {\"url\": \"assets/figures/arxiv/2605.16579v1/fig-006.webp\", \"caption\": \"Table 2: ARL2 achieves consistent efficiency gains with up to 2.26× speedup and significantly reduced memory, while maintaining competitive or improved video quality.\", \"page\": 7, \"index\": 6, \"width\": 495, \"height\": 472, \"caption_zh\": \"表 2：ARL2 实现了稳定的效率提升，最高加速 2.26×，并显著降低内存占用，同时保持了具有竞争力或更优的视频质量。\"}, {\"url\": \"assets/figures/arxiv/2605.16579v1/fig-007.webp\", \"caption\": \"Figure 2: ARL2 attention module (normalization omitted for clarity). Given tokens XN of frame N , QN , KN , VN , which are routed to two branches. The intra-frame branch (top) applies bidirectional softmax attention over tokens within the current frame, producing Ointra. The inter-frame branch (bottom) applies recurrent linear attention, where a fixed-size state S maintains and updates longrange memory across frames, and produces Ointer. The state S is updated once per frame. During training, the hybrid module is learned via a progressive two-stage distillation pipeline (See Sec. 3.4).\", \"page\": 4, \"index\": 7, \"width\": 825, \"height\": 349, \"caption_zh\": \"图 2：ARL2 注意力模块（为清晰起见省略归一化）。给定第 N 帧的 tokens XN、QN、KN、VN，它们被路由到两个分支。帧内分支（上）对当前帧内的 tokens 应用双向 softmax attention，产生 Ointra。帧间分支（下）应用循环 linear attention，其中固定大小的状态 S 维护并更新跨帧的长程记忆，并产生 Ointer。状态 S 每帧更新一次。在训练期间，混合模块通过渐进式两阶段蒸馏流程进行学习（见 Sec. 3.4）。\"}, {\"url\": \"assets/figures/arxiv/2605.16579v1/fig-008.webp\", \"caption\": \"Figure 3: (a) Softmax attention scales quadratically with video length, while the hybrid layer scales linearly. (b) The KV cache grows linearly with video length, whereas ours remains constant. (c) The recurrent state S is shared across all tokens within a frame and updated only after the final clean pass.\", \"page\": 5, \"index\": 8, \"width\": 822, \"height\": 212, \"caption_zh\": \"图 3：（a）Softmax attention 随视频长度呈二次方扩展，而混合层呈线性扩展。（b）KV cache 随视频长度线性增长，而我们的方法保持常数。（c）循环状态 S 在一帧内的所有 tokens 之间共享，并且仅在最后一次 clean pass 之后更新。\"}, {\"url\": \"assets/figures/arxiv/2605.16579v1/fig-009.webp\", \"caption\": \"Figure 7: Qualitative comparison across all evaluated models. Six uniformly sampled frames from 81-frame videos generated with the prompt “A 3D model of a 1800s victorian house.” ARL2 variants maintain consistent appearance and structural detail comparable to the full-softmax baselines while operating with significantly reduced memory and compute.\", \"page\": 16, \"index\": 9, \"width\": 828, \"height\": 825, \"caption_zh\": \"图 7：所有评估模型的定性比较。展示由提示词“A 3D model of a 1800s victorian house.”生成的 81 帧视频中均匀采样的六帧。ARL2 变体在显著降低内存和计算开销的同时，保持了与全 softmax 基线相当的一致外观和结构细节。\"}]"
motivation: 自回归视频扩散适合流式和交互式生成，但传统全 softmax 注意力在长序列下计算和缓存开销过高，限制了可扩展性。
method: ARL2 将注意力分解为帧内局部 softmax 分支和帧间循环线性记忆分支，并通过去噪后更新状态与共享预更新状态避免噪声污染和帧内不对称。
result: "在替换 75% 层为混合线性注意力后，模型最高获得 2.26 倍实际加速和 54% 显存降低，同时保持相近质量并改善时序一致性。"
conclusion: 该工作首次将预训练自回归视频扩散模型高效转换为混合线性注意力架构，为长时程流式视频生成提供了更可扩展的方案。
---

## 摘要
自回归（AR）视频扩散是流式和交互式视频生成的一种强大范式。然而，其对 softmax 自注意力的依赖会导致计算复杂度随序列长度呈二次增长，并因键值缓存而带来内存使用问题，从而限制了其扩展到长视频时域范围的能力。现有补救方法（例如稀疏注意力和 KV 缓存压缩）降低了每步成本，但仍依赖线性增长的缓存，或不可逆地丢弃过去上下文，因此未能解决线性内存增长和流式上下文管理问题。为解决这一可扩展性瓶颈，我们提出 ARL2（Attend Locally, Remember Linearly），一种混合注意力模块，用固定大小的循环状态替代二次复杂度的跨帧注意力。我们将自注意力分解为两个分支：用于空间细节和局部依赖的帧内 softmax 分支，以及维护固定大小状态以支持流式上下文的帧间门控循环线性分支。我们的关键洞见是，softmax 注意力能够捕获细粒度的局部交互，而循环状态提供可控的长程记忆。该设计在实现线性时间缩放和常量内存的同时，相比全 softmax 模型提升了时间一致性。为防止含噪中间状态污染记忆，我们仅在去噪后的传递之后更新循环状态。为避免帧内信息不对称，所有 token 共享相同的预更新状态，而不是进行顺序更新。据我们所知，这是首个通过面向 AR 视频的高效两阶段训练方案，将预训练 AR 视频扩散模型转换为混合线性注意力架构的工作。在将 75% 的层替换为混合线性注意力后，该模型实现了最高 2.26 倍的实际运行时间加速和 54% 的内存降低，同时保持了相当的质量并提升了时间一致性。

## Abstract
Autoregressive (AR) video diffusion is a powerful paradigm for streaming and interactive video generation. However, its reliance on softmax self-attention leads to quadratic compute complexity in sequence length and memory usage due to key-value caching, which limits its scalability to long video horizons. Existing remedies (e.g., sparse attention and KV-cache compression) reduce per-step cost but still rely on a linearly growing cache or irreversibly discard past context, and thus fail to address linear memory growth and streaming context management. To address this scalability bottleneck, we propose ARL2 (Attend Locally, Remember Linearly), a hybrid attention module that replaces quadratic cross-frame attention with a fixed-size recurrent state. We decompose self-attention into two branches: an intra-frame softmax branch for spatial detail and local dependencies, and an inter-frame gated recurrent linear branch that maintains a fixed-size state for streaming context. Our key insight is that softmax attention captures fine-grained local interactions, while a recurrent state provides controllable long-range memory. This design achieves linear-time scaling with constant memory while improving temporal consistency over the full-softmax model. To prevent noisy intermediate states from corrupting memory, we update the recurrent state only after the denoised pass. To avoid within-frame information asymmetry, all tokens share the same pre-update state rather than sequential updates. To the best of our knowledge, this is the first work to convert a pretrained AR video diffusion model into a hybrid linear attention architecture, through an efficient two-stage training scheme for AR video. With 75% of layers replaced by hybrid linear attention, the model achieves up to 2.26 wall-clock speedup and 54% memory reduction, while maintaining comparable quality with improving temporal consistency.