---
title: "AdaState: Self-Evolving Anchors for Streaming Video Generation"
title_zh: AdaState：用于流式视频生成的自演化锚点
authors: "Yusuf Dalva, Pinar Yanardag"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30349v1"
tags: ["query:upstream-ad-planning-idea"]
score: 7.0
evidence: 流式视频扩散的时序演化与一致性
tldr: 本文针对自回归视频扩散模型在流式生成中长期依赖首帧锚点、导致运动和场景演进受抑的问题，提出 AdaState：用随生成过程共同去噪但不渲染的自适应隐状态替代静态首帧锚点，使场景参考随内容动态更新，并通过相对时间建模形成无需额外模块的递归生成机制，从而提升视频动态性与自然演进能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30349v1/fig-001.webp\", \"caption\": \"Figure 7: User study evaluation interface. Each rater views a video generated from a given prompt and scores it on two dimensions using a 5-point Likert scale. Method identity is hidden; video order is randomized. Identifying information has been redacted for anonymity.\", \"page\": 15, \"index\": 1, \"width\": 706, \"height\": 561, \"caption_zh\": \"图 7：用户研究评估界面。每位评分者观看由给定提示生成的视频，并使用 5 点 Likert 量表从两个维度进行评分。方法身份被隐藏；视频顺序随机化。可识别信息已被隐去以保证匿名性。\"}, {\"url\": \"assets/figures/arxiv/2605.30349v1/fig-002.webp\", \"caption\": \"Figure 4: Qualitative comparison across anchor categories. Top block: 12-second generation sampled at 3-second intervals. Bottom block: 30-second generation sampled at 7.5-second intervals. Each block pairs AdaState against one exemplar per baseline category (no reference, EMA reference, static reference); the six exemplars across the two blocks cover all baseline groups. Methods without a persistent anchor accumulate color drift (Self-Forcing, top) or collapse to artifacts at long horizons (Causal Forcing, bottom). EMA and static references prevent collapse but freeze the scene: MemRoPE, Infinity-RoPE, Reward Forcing, and Rolling-Forcing each reproduce a near-identical composition across the entire rollout. AdaState alone preserves identity while producing natural scene evolution.\", \"page\": 7, \"index\": 2, \"width\": 826, \"height\": 583, \"caption_zh\": \"图 4：跨锚点类别的定性比较。上方区块：12 秒生成，以 3 秒间隔采样。下方区块：30 秒生成，以 7.5 秒间隔采样。每个区块将 AdaState 与每个基线类别（无参考、EMA 参考、静态参考）中的一个代表样例配对；两个区块中的六个样例覆盖所有基线组。没有持久锚点的方法会累积颜色漂移（Self-Forcing，上方）或在长时域下坍缩为伪影（Causal Forcing，下方）。EMA 和静态参考可防止坍缩，但会冻结场景：MemRoPE、Infinity-RoPE、Reward Forcing 和 Rolling-Forcing 在整个 rollout 中均复现近乎相同的构图。只有 AdaState 在保留身份一致性的同时产生自然的场景演化。\"}, {\"url\": \"assets/figures/arxiv/2605.30349v1/fig-003.webp\", \"caption\": \"Figure 2: The anchor-recency structure of streaming video attention. (a) Off-diagonal attention in Self-Forcing across chunk depths. The anchor at position 0 (squares) and the freshest chunk frame (triangles) consistently dominate; remaining positions receive roughly uniform mass. (b) 5-second generation on the same prompt. Without a persistent reference, coherence degrades over time. A static reference preserves identity but freezes the scene. AdaState maintains identity while the camera moves and the environment evolves.\", \"page\": 3, \"index\": 3, \"width\": 826, \"height\": 233, \"caption_zh\": \"图 2：流式视频注意力的锚点-近因结构。(a) Self-Forcing 在不同 chunk 深度下的非对角注意力。位置 0 的锚点（方块）和最新的 chunk 帧（三角形）始终占主导；其余位置获得大致均匀的注意力质量。(b) 在相同提示下的 5 秒生成。没有持久参考时，一致性会随时间退化。静态参考可保持身份一致性，但会冻结场景。AdaState 在摄像机移动和环境演化的同时维持身份一致性。\"}, {\"url\": \"assets/figures/arxiv/2605.30349v1/fig-004.webp\", \"caption\": \"Table 1: Evaluation in 5s and 30s rollouts. 5 seconds (21 frames) is within all methods’ training horizons; 30 seconds (120 frames) is six times the training length. VisionReward (VR) is reported at 5 seconds only. Best in bold, second best underlined.\", \"page\": 8, \"index\": 4, \"width\": 826, \"height\": 366, \"caption_zh\": \"表 1：5s 和 30s rollout 的评估。5 秒（21 帧）位于所有方法的训练时域内；30 秒（120 帧）是训练长度的六倍。VisionReward (VR) 仅报告 5 秒结果。最佳结果以粗体表示，次优结果以下划线表示。\"}, {\"url\": \"assets/figures/arxiv/2605.30349v1/fig-005.webp\", \"caption\": \"Figure 3: AdaState Framework. The adaptive state (green) is denoised alongside content at each chunk but never rendered. Its clean KV is written to position 0 and carried to subsequent chunks via the state recurrence (green dashed). Decoded state previews (middle, green-bordered, matching the state tokens) visualize the hidden state in image space; the zoom insets reveal the model’s denoising errors, which the architecture surfaces at the cache anchor rather than letting a clean reference absorb them silently. The bottom strip (blue-bordered, matching the video content tokens) shows the cleanly denoised video output, with continuous scene progression from t=0 to t=12.\", \"page\": 4, \"index\": 5, \"width\": 808, \"height\": 408, \"caption_zh\": \"图 3：AdaState 框架。自适应状态（绿色）在每个 chunk 中与内容一起去噪，但从不被渲染。其干净 KV 被写入位置 0，并通过状态递归（绿色虚线）传递到后续 chunk。解码后的状态预览（中间，绿色边框，与状态 token 对应）在图像空间中可视化隐藏状态；放大插图揭示了模型的去噪误差，该架构将这些误差显式暴露在缓存锚点处，而不是让一个干净参考静默地吸收它们。底部条带（蓝色边框，与视频内容 token 对应）展示了干净去噪后的视频输出，场景从 t=0 到 t=12 连续推进。\"}, {\"url\": \"assets/figures/arxiv/2605.30349v1/fig-006.webp\", \"caption\": \"Table 7: Representative prompts from the 30-second evaluation set.\", \"page\": 17, \"index\": 6, \"width\": 791, \"height\": 360, \"caption_zh\": \"表 7：来自 30 秒评估集的代表性提示。\"}, {\"url\": \"assets/figures/arxiv/2605.30349v1/fig-007.webp\", \"caption\": \"Table 4: Full VBench breakdown at 5 seconds (21 frames). VisionReward (VR) is reported ×10. Best in bold, second best underlined.\", \"page\": 16, \"index\": 7, \"width\": 827, \"height\": 321, \"caption_zh\": \"表 4：5 秒（21 帧）下完整的 VBench 分项结果。VisionReward (VR) 以 ×10 报告。最佳结果以粗体表示，次优结果以下划线表示。\"}, {\"url\": \"assets/figures/arxiv/2605.30349v1/fig-008.webp\", \"caption\": \"Table 5: Full VBench breakdown at 30 seconds (120 frames, 6× training horizon). Best in bold, second best underlined.\", \"page\": 16, \"index\": 8, \"width\": 830, \"height\": 310, \"caption_zh\": \"表 5：30 秒（120 帧，6× 训练时域）下完整的 VBench 分项结果。最佳结果以粗体表示，次优结果以下划线表示。\"}, {\"url\": \"assets/figures/arxiv/2605.30349v1/fig-009.webp\", \"caption\": \"Table 6: Representative prompts from the 5-second evaluation set.\", \"page\": 16, \"index\": 9, \"width\": 791, \"height\": 381, \"caption_zh\": \"表 6：来自 5 秒评估集的代表性提示。\"}]"
motivation: 现有流式视频生成过度依赖首帧作为固定锚点，容易造成画面静态、运动不足和视角演进受限。
method: AdaState 引入一个随每个视频块共同去噪的隐藏自适应状态，通过关注前一状态与当前内容来生成动态场景锚点，并采用相对时间结构实现递归式状态转移。
result: 实验表明，自适应状态能显著增强生成视频中的运动表现、相机变化和自然场景推进。
conclusion: AdaState 通过让锚点自我演化，有效缓解首帧锁定问题，为流式视频扩散模型提供了更动态且无需额外模块的生成范式。
---

## 摘要
自回归视频扩散模型通过逐帧生成并使每个片段以先前生成的内容为条件来生成流式视频。这些模型在结构上锚定于第一帧：其键值表示在注意力缓存中占据特权位置，并在整个生成过程中作为主要的场景参照。作为缓存中最干净、误差最少的位置，该锚点会吸引不成比例的注意力，从而抑制视频动态，并将场景构图锁定在初始视角，即便场景会自然演化。其结果是生成的视频在时间维度上较为浅薄，其中运动、相机移动和场景推进都被削弱，以换取静态一致性。为解决这一问题，我们用一种自适应状态替代静态锚点；该状态是一种隐藏潜变量，模型在每个片段中将其与内容一同去噪，但从不将其渲染出来。模型不再引用冻结的第一帧，而是通过同时关注先前状态和当前内容，在每一步生成自身的场景锚点，从而产生一个随生成内容共同演化的参照。不同于编码绝对时间概念的标准视频生成方法，我们的表述将时间视为相对的：无论生成已推进多远，每个生成步骤都看到相同的位置结构，并且每个片段的状态转移都是相同的。这些性质共同在生成过程中引入了递归，其中去噪作为转移函数，KV 缓存作为载体，且无需外部模块。实验表明，自适应状态显著改善了视频动态，使生成视频能够呈现更丰富的运动和更自然的场景推进。

## Abstract
Autoregressive video diffusion models generate streaming video by producing frames sequentially, conditioning each chunk on previously generated content. These models are structurally anchored to the first frame: its key-value representation occupies a privileged position in the attention cache and serves as the primary scene reference throughout generation. As the cleanest and most error-free position in the cache, this anchor draws disproportionate attention, suppressing video dynamics, and locking scene composition to the initial viewpoint even as the scene naturally evolves. The result is a temporally shallow video in which motion, camera movement, and scene progression are dampened in favor of static consistency. To address this, we replace the static anchor with an adaptive state, a hidden latent that the model denoises alongside content at every chunk but never renders. Rather than referencing a frozen first frame, the model generates its own scene anchor at each step by attending to both the previous state and the current content, producing a reference that evolves with the generated content. Unlike standard video generation, which encodes an absolute notion of time, our formulation treats time as relative: every generation step sees the same positional structure regardless of how far generation has progressed, and the state transition is identical at every chunk. Together, these properties introduce a recurrence into the generation process, where denoising serves as the transition function, and the KV cache serves as the carrier, requiring no external module. Experiments demonstrate that the adaptive state substantially improves video dynamics, enabling richer motion and natural scene progression within generated videos.