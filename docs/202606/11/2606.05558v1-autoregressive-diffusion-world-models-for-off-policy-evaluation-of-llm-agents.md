---
title: Autoregressive Diffusion World Models for Off-Policy Evaluation of LLM Agents
title_zh: 用于 LLM 智能体离策略评估的自回归扩散世界模型
authors: "Kaixuan Liu, Guojun Xiong, Weinan Zhang, Shengpu Tang"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05558v1"
tags: ["query:upstream-ad-planning-idea"]
score: 6.0
evidence: 基于轨迹的潜在扩散世界模型
tldr: 本文针对LLM智能体在多轮交互环境中在线评估成本高且存在风险的问题，提出ADWM离线评估框架。该方法利用预收集轨迹学习自回归扩散世界模型，按因果顺序交替模拟环境响应与智能体决策，并通过策略条件化评分引导每步生成，从而在不真实执行新策略的情况下估计其性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05558v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of evaluation paradigms for LLM agents. (Left) On-policy evaluation requires executing the agent in the real environment, which is expensive and potentially unsafe. (Middle) Traditional off-policy evaluation learns a model-based simulator from offline data, but suffers from two fundamental issues: distribution shift between the behavior and target policies, and compounding error accumulated over multi-step rollouts. (Right) Our ADWMADWM addresses both issues simultaneously. Policy guidance injects πe into every denoising step to alleviate distribution shift, while the diffusion prior combined with action-aware denoising alleviates compounding error.\", \"page\": 2, \"index\": 1, \"width\": 972, \"height\": 227, \"caption_zh\": \"图 1：LLM agent 评估范式的比较。（左）On-policy evaluation 需要在真实环境中执行 agent，成本高且可能不安全。（中）传统 off-policy evaluation 从离线数据中学习基于模型的模拟器，但存在两个根本问题：behavior policy 与 target policy 之间的分布偏移，以及多步 rollout 中累积的复合误差。（右）我们的 ADWMADWM 同时解决这两个问题。Policy guidance 在每个去噪步骤中注入 πe 以缓解分布偏移，而 diffusion prior 结合 action-aware denoising 可缓解复合误差。\"}, {\"url\": \"assets/figures/arxiv/2606.05558v1/fig-002.webp\", \"caption\": \"Table 2: Per-cell Spearman ρ between each estimator’s Ĵ and the real-environment ground-truth curve. ADWM is the only estimator with ρ>0 on every configuration (mean +0.82, min +0.67). CI95 and p-values from 2000 bootstrap resamples on the (Ĵ ,GT) pairs. Per-seed values and the underlying Ĵ / GT curves are in Appendix H.\", \"page\": 8, \"index\": 2, \"width\": 786, \"height\": 288, \"caption_zh\": \"表 2：每个单元中各估计器的 Ĵ 与真实环境 ground-truth 曲线之间的 Spearman ρ。ADWM 是唯一在所有配置上均达到 ρ>0 的估计器（均值 +0.82，最小值 +0.67）。CI95 和 p 值基于 (Ĵ ,GT) 对的 2000 次 bootstrap 重采样获得。逐 seed 数值以及对应的 Ĵ / GT 曲线见附录 H。\"}, {\"url\": \"assets/figures/arxiv/2606.05558v1/fig-003.webp\", \"caption\": \"Figure 3: Training loss curves across all four benchmarks. Total world-model loss LWM and its components converge stably within 50 epochs; see Appendix F.7 for full definitions and Appendix F.8 for the training procedure.\", \"page\": 8, \"index\": 3, \"width\": 970, \"height\": 216, \"caption_zh\": \"图 3：四个基准上的训练损失曲线。总 world-model 损失 LWM 及其各组成部分在 50 个 epoch 内稳定收敛；完整定义见附录 F.7，训练流程见附录 F.8。\"}, {\"url\": \"assets/figures/arxiv/2606.05558v1/fig-004.webp\", \"caption\": \"Figure 2: Architecture of ADWM. Offline trajectories are encoded by E into latent states zt, which are processed by a diffusion world model pθ through K-step denoising. A projector Gψ maps each latent to soft tokens õt that the evaluation policy πe can read in its own embedding space. πe plays two complementary roles: it steers the denoising process via policy guidance (dashed arrow, Section 4.2), and samples actions at conditioned on õt to drive autoregressive rollout (solid arrow). A reward head rρ predicts rewards r̂t used in the Monte Carlo value estimate Ĵ(πe).\", \"page\": 4, \"index\": 4, \"width\": 972, \"height\": 258, \"caption_zh\": \"图 2：ADWM 的架构。离线轨迹由 E 编码为潜在状态 zt，并由 diffusion world model pθ 通过 K 步去噪进行处理。投影器 Gψ 将每个潜变量映射为 soft tokens õt，使 evaluation policy πe 能够在其自身的 embedding space 中读取。πe 扮演两个互补角色：通过 policy guidance（虚线箭头，第 4.2 节）引导去噪过程，并在以 õt 为条件时采样动作 at，以驱动自回归 rollout（实线箭头）。reward head rρ 预测奖励 r̂t，用于 Monte Carlo 价值估计 Ĵ(πe)。\"}, {\"url\": \"assets/figures/arxiv/2606.05558v1/fig-005.webp\", \"caption\": \"Table 4: Per-module parameter counts in the ADWM world model (7.38M parameters total for dz=64).\", \"page\": 17, \"index\": 5, \"width\": 740, \"height\": 267, \"caption_zh\": \"表 4：ADWM world model 中各模块的参数量（当 dz=64 时总计 7.38M 参数）。\"}, {\"url\": \"assets/figures/arxiv/2606.05558v1/fig-006.webp\", \"caption\": \"Figure 4: Component ablation on three environments (avg-Ĵ Spearman ρ, single seed; five-seed trends in Appendix I). Each component dominates in a distinct reward regime: local CFG on sparse long-horizon tasks, continuation guidance on goal-conditioned trajectories, and the ψ adapter on linguistically rich state spaces. Removing any single component degrades at least one environment; full ADWM consistently achieves the highest ρ.\", \"page\": 9, \"index\": 6, \"width\": 892, \"height\": 304, \"caption_zh\": \"图 4：三个环境上的组件消融（avg-Ĵ Spearman ρ，单个 seed；五个 seed 的趋势见附录 I）。每个组件在不同的奖励机制中占主导：local CFG 用于稀疏长时域任务，continuation guidance 用于目标条件轨迹，ψ adapter 用于语言信息丰富的状态空间。移除任何单个组件都会使至少一个环境的性能下降；完整 ADWM 始终取得最高的 ρ。\"}]"
motivation: 在线评估LLM智能体需要真实环境交互，代价高、风险大，因此需要可靠的离线策略评估方法。
method: ADWM将每个状态转移建模为独立的扩散去噪过程，并在逐步 rollout 中由待评估LLM策略条件化引导环境生成。
result: 实验表明ADWM在多种多轮智能体任务中能给出准确的价值估计，并具备较好的评估可靠性。
conclusion: ADWM为离线评估LLM智能体提供了一种实用且有效的扩散世界模型框架。
---

## 摘要
在多轮交互环境中评估大语言模型（LLM）智能体既昂贵又具有风险，因为这需要与在线环境进行交互。我们提出了 ADWM（自回归扩散世界模型），这是一种仅利用预先收集的轨迹来估计新 LLM 智能体策略性能的评估框架。其核心思想是学习一个潜在扩散世界模型，用于模拟环境如何响应待评估策略，而无需在真实环境中实际执行该策略。现有基于扩散的离策略评估（OPE）方法通过联合扩散状态和动作，以单次过程引导完整轨迹；然而，对于 LLM 智能体而言，其动作是离散文本，必须在观察环境后由策略采样得到，因此这一假设不再成立。不同于容易受到误差累积影响的自回归世界模型，ADWM 将每个转移建模为独立的去噪过程，从而实现可靠的逐步展开，使世界模型与智能体按因果顺序交替运行。关键在于，待评估的 LLM 智能体在每一步通过策略条件化的得分函数直接引导扩散生成，确保模拟轨迹能够准确反映其决策模式。实证结果表明，ADWM 在多种多轮智能体任务中能够获得准确的价值估计和可靠的评估结果，展示了其作为离线 LLM 智能体评估实用框架的潜力。

## Abstract
Evaluating large language model (LLM) agents in multi-turn interactive environments is expensive and risky, as it requires online environment interaction. We propose ADWM (Autoregressive Diffusion World Model), an evaluation framework that estimates the performance of a new LLM agent policy purely from pre-collected trajectories. The core idea is to learn a latent diffusion world model that simulates how the environment responds to the evaluation policy, without ever executing it in the real environment. Existing diffusion-based OPE methods guide full trajectories in a single pass by jointly diffusing states and actions, an assumption that breaks down for LLM agents whose actions are discrete text that must be sampled from the policy after observing the environment. Unlike autoregressive world models that suffer from compounding errors, ADWM models each transition as an independent denoising process, enabling reliable step-by-step rollouts where the world model and agent alternate in causal order. Crucially, the LLM agent under evaluation directly guides the diffusion generation at each step via a policy-conditioned score function, ensuring that simulated trajectories accurately reflect its decision-making patterns. Empirically, ADWM achieves accurate value estimates and evaluation reliability across diverse multi-turn agent tasks, demonstrating its promise as a practical framework for offline LLM agent evaluation.