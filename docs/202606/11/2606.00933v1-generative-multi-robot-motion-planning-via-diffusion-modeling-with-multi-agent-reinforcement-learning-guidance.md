---
title: Generative Multi-Robot Motion Planning via Diffusion Modeling with Multi-Agent Reinforcement Learning Guidance
title_zh: 基于多智能体强化学习引导的扩散建模生成式多机器人运动规划
authors: "Suk Ki Lee, Venkata Sai Deepak Mutta, Hyunwoong Ko"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00933v1"
tags: ["query:q7"]
score: 8.0
evidence: 扩散式生成轨迹规划
tldr: 本文面向多机器人共享环境中的协同运动规划问题，提出将去中心化扩散轨迹生成与多智能体强化学习价值引导结合的框架。各机器人用单智能体数据训练的扩散模型独立生成可行候选轨迹，再通过集中式MARL价值函数在反向扩散中进行梯度引导，从而在无需联合规划或重训生成模型的情况下降低机器人间冲突。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00933v1/fig-001.webp\", \"caption\": \"FIGURE 4: Training loss of the single-agent diffusion model over 40 epochs.\", \"page\": 8, \"index\": 1, \"width\": 404, \"height\": 394, \"caption_zh\": \"图 4：单智能体扩散模型在 40 个 epoch 内的训练损失。\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-002.webp\", \"caption\": \"FIGURE 2: Experimental setup for the proposed framework, illustrated through the compositional distribution p̃θ (X ) ∝ pθ (X ) exp(λVφ (X )). For the motion prior pθ , a single agent is trained via RL in a 30×30 m custom maze environment in Isaac Sim, where internal walls create constrained passages requiring feasible path planning between start and goal locations. The collected trajectories are used to train the diffusion model. For the coordination value exp(λVφ (X )), four agents are trained via multi-agent RL (MARL) in an open space environment, learning a centralized value function that estimates coordination quality from the joint observations of all agents. At inference time, the two components are composed without retraining, producing guided trajectories p̃θ (X ) in which multiple agents navigate the maze environment with reduced inter-agent interference.\", \"page\": 8, \"index\": 2, \"width\": 904, \"height\": 468, \"caption_zh\": \"图 2：所提出框架的实验设置，通过组合分布 p̃θ (X ) ∝ pθ (X ) exp(λVφ (X )) 进行说明。对于运动先验 pθ，在 Isaac Sim 中的 30×30 m 自定义迷宫环境中通过 RL 训练单个智能体，其中内部墙体形成受限通道，要求在起点和目标位置之间进行可行路径规划。收集到的轨迹用于训练扩散模型。对于协调价值 exp(λVφ (X ))，在开放空间环境中通过多智能体 RL（MARL）训练四个智能体，学习一个集中式价值函数，该函数根据所有智能体的联合观测估计协调质量。在推理时，两个组成部分无需重新训练即可组合，生成引导轨迹 p̃θ (X )，使多个智能体在迷宫环境中导航并减少智能体间干扰。\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-003.webp\", \"caption\": \"FIGURE 6: Comparison of multi-agent trajectories in the maze environment. (a) Trajectories generated by the baseline planner using diffusion without guidance. (b) Trajectories generated by the diffusion planner with MARL-based guidance. Each color denotes one robot navigating from the lower-left start region to the upper-right goal region. Star and circle markers indicate start and goal positions, respectively. The guided planner produces greater spatial separation among agents while following a similar global route.\", \"page\": 10, \"index\": 3, \"width\": 1046, \"height\": 468, \"caption_zh\": \"图 6：迷宫环境中多智能体轨迹的比较。(a) 基线规划器使用无引导扩散生成的轨迹。(b) 采用基于 MARL 的引导的扩散规划器生成的轨迹。每种颜色表示一个机器人从左下方起始区域导航至右上方目标区域。星形和圆形标记分别表示起点和目标位置。引导式规划器在遵循相似全局路线的同时，使智能体之间产生更大的空间分离。\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-004.webp\", \"caption\": \"FIGURE 1: Overall architecture of the proposed framework. In the single-agent data collection phase, an RL policy learns to solve the task in the environment, and the resulting diverse trajectories are used to train a diffusion model that learns the underlying motion distribution, enabling goal-conditioned trajectory generation for a single agent. In the centralized value function learning phase, a value function is trained via multi-agent RL (MARL) to estimate coordination quality from the global state of all agents, with learned feedback propagated back to each agent’s policy. At inference time, the trained diffusion model is shared across all agents without retraining, and each agent independently generates a trajectory conditioned on its own start-goal pair. The independently generated trajectories are then guided by the value function gradient at each denoising step, steering the sampling process toward coordinated multi-agent behaviors and producing the final trajectory set with reduced inter-agent interference.\", \"page\": 4, \"index\": 4, \"width\": 1129, \"height\": 968, \"caption_zh\": \"图 1：所提出框架的整体架构。在单智能体数据收集阶段，RL 策略学习在环境中完成任务，并将所得的多样化轨迹用于训练扩散模型，使其学习底层运动分布，从而实现单智能体的目标条件轨迹生成。在集中式价值函数学习阶段，通过多智能体 RL（MARL）训练价值函数，以根据所有智能体的全局状态估计协调质量，并将学习到的反馈传播回每个智能体的策略。在推理时，训练好的扩散模型在所有智能体之间共享且无需重新训练，每个智能体基于自身的起点—目标对独立生成轨迹。随后，在每个去噪步骤中，利用价值函数梯度对独立生成的轨迹进行引导，将采样过程导向协调的多智能体行为，并生成具有更少智能体间干扰的最终轨迹集合。\"}]"
motivation: 多机器人运动规划需要兼顾可扩展性与智能体交互，而集中式方法难扩展、去中心化方法又容易忽略相互冲突。
method: 每个机器人独立使用扩散模型生成轨迹，并利用MARL训练的集中式价值函数通过指数倾斜和梯度引导反向扩散过程。
result: "在四机器人迷宫仿真中，价值引导扩散规划将智能体间干扰率从55.4%降低到41.8%。"
conclusion: MARL价值引导能在保持去中心化生成规划可扩展性的同时有效引入多机器人协调能力。
---

## 摘要
在共享环境中协调多个机器人，需要在考虑智能体之间相互作用的同时，为每个智能体生成可行轨迹。随着机器人数量的增加，集中式规划方法变得难以扩展；而允许每个智能体独立规划的分散式方法本身并不能考虑智能体间相互作用。本文提出了一种协调式多机器人运动规划框架，将分散式生成轨迹规划与基于多智能体强化学习（MARL）的协调相结合。每个机器人使用在单智能体运动数据上训练的扩散模型独立生成候选轨迹，利用生成模型产生可行且多样轨迹的能力。为减少智能体之间的冲突，通过 MARL 训练得到的集中式价值函数以基于梯度的引导方式指导反向扩散过程，从而在无需集中式联合规划或重新训练生成模型的情况下，实现具备交互感知的轨迹生成。该引导遵循指数倾斜公式，其中价值函数将去噪分布偏置到具有更高期望多智能体回报的轨迹。该框架在包含四个移动机器人的仿真迷宫环境中进行了评估。实验结果表明，所提出的价值引导扩散规划将智能体间干扰率从 55.4% 降低到 41.8%，证明在保持分散式轨迹生成可扩展性的同时，可以有效实现协调。这些结果表明，基于 MARL 的价值引导能够在不需要完全联合的多机器人模型的情况下，有效地将协调机制引入分散式生成规划器。

## Abstract
Coordinating multiple robots in shared environments requires generating feasible trajectories for each agent while accounting for interactions among agents. Centralized planning approaches become difficult to scale as the number of robots increases, while decentralized approaches that allow each agent to plan independently do not inherently account for inter-agent interactions. This paper presents a framework for coordinated multi-robot motion planning that combines decentralized generative trajectory planning with multi-agent reinforcement learning (MARL)-based coordination. Each robot independently generates candidate trajectories using a diffusion model trained on single-agent motion data, leveraging the generative model's ability to produce feasible and diverse trajectories. To reduce conflicts between agents, a centralized value function trained via MARL guides the reverse diffusion process through gradient-based steering, enabling interaction-aware trajectory generation without centralized joint planning or retraining of the generative model. This guidance follows an exponential tilting formulation, in which the value function biases the denoising distribution toward trajectories with higher expected multi-agent return. The framework is evaluated in a simulated maze environment with four mobile robots. Experimental results show that the proposed value-guided diffusion planning reduces the inter-agent interference rate from 55.4% to 41.8%, demonstrating that coordination can be effectively achieved while preserving the scalability of decentralized trajectory generation. These results suggest that MARL-based value guidance can effectively introduce coordination into decentralized generative planners without requiring a fully joint multi-robot model.