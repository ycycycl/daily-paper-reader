---
title: "FlexPath: Learned Semantic Path Priors for Image-Based Planning"
title_zh: FlexPath：用于基于图像规划的学习型语义路径先验
authors: "Taehyoung Kim, Tim Schoenbrod, David Eckel, Henri Meeß"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10167v1"
tags: ["query:q2"]
score: 6.5
evidence: 图像规划的学习路径先验
tldr: FlexPath面向基于图像地图的学习型路径规划，解决现有方法过度绑定最短路径监督、难以适配多样化目标的问题。它通过两阶段框架先学习任务无关的可行路径空间先验，再用可微路径形状目标进行任务级偏好调整，从而在无需重学路径结构的情况下支持最短路、避障清距、语义避让和路径点引导等目标，并保持与经典搜索规划器兼容。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10167v1/fig-001.webp\", \"caption\": \"Fig. 3: Qualitative comparison on TMP 640k. Selected examples of the pathfinding results. Expanded nodes are shown in green, and the resulting path is shown in red. PPMs shown only for methods that produce them.\", \"page\": 11, \"index\": 1, \"width\": 725, \"height\": 206, \"caption_zh\": \"图 3：在 TMP 640k 上的定性比较。展示路径规划结果的若干示例。扩展节点以绿色显示，生成路径以红色显示。仅对会生成 PPM 的方法展示 PPM。\"}, {\"url\": \"assets/figures/arxiv/2606.10167v1/fig-002.webp\", \"caption\": \"Fig. 1: One prior, flexible path adaptation. A task-agnostic soft path mask (left) is fine-tuned via PSO to yield shortest-path, obstacle-avoidance, and waypointconstrained routes (via WP) without training from scratch.\", \"page\": 2, \"index\": 2, \"width\": 725, \"height\": 196, \"caption_zh\": \"图 1：一个先验，灵活的路径适配。一个任务无关的软路径掩码（左）通过 PSO 进行微调，从而在无需从头训练的情况下生成最短路径、避障路径和经由航点约束的路线（via WP）。\"}, {\"url\": \"assets/figures/arxiv/2606.10167v1/fig-003.webp\", \"caption\": \"Table 1: Shortest-path and zero-shot generalization results. All models trained on TMP 640k. PT: Stage 1 only, FT: Stage 2 only, PT+FT: full pipeline.\", \"page\": 10, \"index\": 3, \"width\": 725, \"height\": 434, \"caption_zh\": \"表 1：最短路径与零样本泛化结果。所有模型均在 TMP 640k 上训练。PT：仅 Stage 1，FT：仅 Stage 2，PT+FT：完整流程。\"}, {\"url\": \"assets/figures/arxiv/2606.10167v1/fig-004.webp\", \"caption\": \"Table 3: Obstacle-clearance evaluation on TMP 640k. PT and PT+FT share the same Stage 1 pretrained prior. Full Avoidance is computed on feasible instances for each dmin.\", \"page\": 13, \"index\": 4, \"width\": 725, \"height\": 260, \"caption_zh\": \"表 3：在 TMP 640k 上的障碍物间隙评估。PT 和 PT+FT 共享相同的 Stage 1 预训练先验。Full Avoidance 在每个 dmin 的可行实例上计算。\"}, {\"url\": \"assets/figures/arxiv/2606.10167v1/fig-005.webp\", \"caption\": \"Table 2: Ablation studies on TMP 640k. Each row modifies one component relative to the Stage 1 baseline (Weighted BCE, RGB, UNet+Att).\", \"page\": 12, \"index\": 5, \"width\": 725, \"height\": 234, \"caption_zh\": \"表 2：在 TMP 640k 上的消融研究。每一行相对于 Stage 1 基线（Weighted BCE, RGB, UNet+Att）修改一个组件。\"}, {\"url\": \"assets/figures/arxiv/2606.10167v1/fig-006.webp\", \"caption\": \"Fig. 2: Overview of FlexPath. Stage 1 (top): Given an input map X, a neural network fθ predicts a raw path field P (logits), learning a task-agnostic feasibility prior via weighted binary cross-entropy against rasterized planner demonstrations Y. Stage 2 (bottom): The prior is adapted via differentiable Path Shape Objectives (PSOs) encoding task-specific preferences (e.g., shortest-path cost or obstacle clearance). Stage 2 is weakly supervised: Y provides only a scalar cost budget (not spatial targets), while path shape is determined by PSO functionals on the soft path mask C.\", \"page\": 5, \"index\": 6, \"width\": 721, \"height\": 380, \"caption_zh\": \"图 2：FlexPath 概述。Stage 1（上）：给定输入地图 X，神经网络 fθ 预测原始路径场 P（logits），通过针对栅格化规划器示范 Y 的加权二元交叉熵学习任务无关的可行性先验。Stage 2（下）：该先验通过可微的 Path Shape Objectives (PSOs) 进行适配，以编码任务特定偏好（例如最短路径代价或障碍物间隙）。Stage 2 采用弱监督：Y 仅提供标量代价预算（而非空间目标），路径形状则由软路径掩码 C 上的 PSO 泛函确定。\"}]"
motivation: 现有学习型规划器通常隐含优化最短路径，缺乏对清距、语义偏好等替代规划目标的灵活适配能力。
method: FlexPath先用模仿学习从视觉地图中学习可行路径先验，再通过可微Path Shape Objectives对该先验进行目标级适配。
result: "在最短路径任务上相比TransPath将TMP搜索开销降低14.3%，并在未见领域表现出强零样本泛化，同时在清距为2的避障任务中实现96.8%的完全避障率。"
conclusion: FlexPath通过解耦可行性与偏好，实现了一个可复用、可适配多目标且能与经典规划器结合的图像路径规划框架。
---

## 摘要
近年来，基于学习的路径规划器使用神经网络处理视觉地图表示，并为经典搜索算法近似启发式函数，从而以更低的搜索开销生成近似最优路径。然而，这些方法受限于其监督信号中隐含的最短路径目标，限制了其适应其他准则的灵活性。我们提出 FlexPath，这是一种将可行性与偏好解耦的两阶段框架。在第 1 阶段，我们使用模仿学习，从视觉地图输入中学习与任务无关的可行路径空间先验。在第 2 阶段，可微分的路径形状目标（Path Shape Objectives, PSOs）将该先验适配到特定任务准则，而无需重新学习路径结构，仅需进行高效的目标层面适配。单个预训练模型即可适配多个目标。对于最短路径规划，与最先进的 TransPath 相比，FlexPath 在 TMP 上将搜索开销降低了 14.3%，同时平均找到成本更低的路径，并在三个未见过的领域中展现出强大的零样本泛化能力。对于最小净空距离为 2 的障碍物净空任务，它实现了 96.8% 的完全避障率，同时保持较低的搜索成本。该框架还可通过目标层面适配扩展到语义感知避障和航点引导，并在推理时保持与经典规划器的兼容性。数据和代码可在 https://github.com/FraunhoferIVI/FlexPath 获取。

## Abstract
Recent learning-based path planners use neural networks to process visual map representations and approximate heuristics for classical search algorithms, yielding near-optimal paths with reduced search effort. However, these methods are tied to the shortest-path objective implicit in their supervision, which limits their flexibility to accommodate alternative criteria. We introduce FlexPath, a two-stage framework that decouples feasibility from preference. In Stage 1, we use imitation learning to acquire a task-independent spatial prior over feasible paths from visual map inputs. In Stage 2, differentiable Path Shape Objectives (PSOs) adapt this prior toward task-specific criteria without relearning path structure, requiring only efficient objective-level adaptation. A single pretrained model can be adapted to multiple objectives. For shortest-path planning, FlexPath reduces search effort on TMP by 14.3% compared to the state-of-the-art TransPath, while also finding lower-cost paths on average and demonstrating strong zero-shot generalization across three unseen domains. For obstacle clearance with minimum clearance distance 2, it achieves 96.8% full obstacle avoidance while maintaining low search cost. The framework further extends to semantic-aware avoidance and waypoint guidance via objective-level adaptation, and remains compatible with classical planners at inference time. Data and code are available at https://github.com/FraunhoferIVI/FlexPath.