---
title: "IMWM: Intuition Models Complement World Models for Latent Planning"
title_zh: IMWM：直觉模型为潜在空间规划中的世界模型提供互补
authors: "Baoqi Gao, Ruize Han, Miao Wang, Song Wang"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01626v1"
tags: ["query:upstream-ad-planning-idea"]
score: 8.5
evidence: 结合动作直觉的世界模型潜空间规划
tldr: 本文研究基于像素输入的潜空间世界模型规划，指出即使世界模型近似完美，有限预算采样规划器仍可能因搜索不足而失败。为此提出 IMWM，将由示范训练的直觉模型与世界模型结合，通过检索初始化、混合代价和可靠性门控引导规划搜索，在多个目标到达任务中显著提升成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01626v1/fig-001.webp\", \"caption\": \"Figure 4: Intuition-model training (per task, CLIP-style). Rows are start–goal queries and columns are schematic action-chunk keys for one OGBench-Cube task-specific batch. Diagonal cells pair each query with its own action chunk and are the InfoNCE positives; off-diagonal cells pair the query with other same-task action chunks and are in-batch negatives. The real OGBench-Cube thumbnails are illustrative observation examples; they are not claimed to be the exact training-bank samples.\", \"page\": 6, \"index\": 1, \"width\": 664, \"height\": 469, \"caption_zh\": \"图 4：直觉模型训练（按任务，CLIP 风格）。行表示起点–目标查询，列表示一个 OGBench-Cube 任务特定批次中的示意性动作片段键。对角线单元格将每个查询与其自身的动作片段配对，作为 InfoNCE 正样本；非对角线单元格将该查询与其他同任务动作片段配对，作为批内负样本。真实的 OGBench-Cube 缩略图仅作为观测示例说明；并不声称它们是精确的训练库样本。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-002.webp\", \"caption\": \"Figure 3: Intuition-model scoring. The (start, goal) observations are encoded into a query q(zI0 , z I g) and a candidate action chunk into a key k(a); their compatibility D = q · k/τ scores how well the action fits the start→goal transition (higher is more promising). The start/goal panels are real OGBench-Cube observations; the action chunk is drawn schematically.\", \"page\": 6, \"index\": 2, \"width\": 714, \"height\": 506, \"caption_zh\": \"图 3：直觉模型评分。(start, goal) 观测被编码为查询 q(zI0 , z I g)，候选动作片段被编码为键 k(a)；二者的兼容性 D = q · k/τ 用于衡量该动作与 start→goal 转换的匹配程度（值越高越有希望）。起点/目标面板是真实的 OGBench-Cube 观测；动作片段以示意方式绘制。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-003.webp\", \"caption\": \"Figure 1: A world model alone is not enough. Left (World Model only): the planner rolls out candidate actions accurately, but under a finite search budget it cannot find a goal-reaching action in the vast action space. Right (IMWM): pairing the same frozen world model with a frozen intuition model concentrates the search on the promising actions and reaches the goal. Both regimes use the same frozen world model and the same CEM budget; only the proposal, cost, and gating differ.\", \"page\": 2, \"index\": 3, \"width\": 826, \"height\": 358, \"caption_zh\": \"图 1：仅有世界模型是不够的。左（仅 World Model）：规划器能够准确展开候选动作，但在有限搜索预算下，无法在巨大的动作空间中找到到达目标的动作。右（IMWM）：将同一个冻结的世界模型与冻结的直觉模型配对，可将搜索集中到有希望的动作上并到达目标。两种设置使用相同的冻结世界模型和相同的 CEM 预算；仅 proposal、cost 和 gating 不同。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-004.webp\", \"caption\": \"Table 3: Retrieval Initialization × Hybrid Cost (2×3 grid; mean cell success, %, over the 12-cellper-task grid). This grid is gate-faithful rather than a strict orthogonal factorial: the both weights are the gate’s per-task choice and the (on, both) row, marked †, is the deployed full IMWM, whose recipe is gate-selected per task. For Reacher the gate disables retrieval and selects the forward-only recipe, so Reacher’s both entries reduce to its world-model-only fallback and its full-IMWM value (83.8) is statistically tied with the (off, world-model-only) baseline (83.2) up to run-to-run CEM variation. The (off, world-model-only) row is the headline baseline (c1, retrieval off); the (on, worldmodel-only) row adds Retrieval Initialization to the world-model cost (c2). Per-cell paired CIs in Appendix G, Table 12.\", \"page\": 10, \"index\": 4, \"width\": 677, \"height\": 183, \"caption_zh\": \"表 3：Retrieval Initialization × Hybrid Cost（2×3 网格；12-cell-per-task 网格上的平均单元成功率，%）。该网格是 gate-faithful 的，而非严格正交的因子设计：both 权重是门控的逐任务选择；标记为 † 的 (on, both) 行是部署的完整 IMWM，其配方按任务由 gate 选择。对于 Reacher，gate 禁用 retrieval 并选择 forward-only 配方，因此 Reacher 的 both 条目退化为其仅世界模型的回退方案，其完整 IMWM 数值（83.8）与 (off, world-model-only) 基线（83.2）在逐次运行的 CEM 变异范围内统计上持平。(off, world-model-only) 行是主要基线（c1，retrieval off）；(on, worldmodel-only) 行在世界模型 cost 上加入 Retrieval Initialization（c2）。逐单元配对 CI 见附录 G，表 12。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-005.webp\", \"caption\": \"Figure 5: The four pixel-based goal-reaching tasks. For each task we show two real evaluation frames: the start observation o0 and the benchmark-provided goal observation og (the goal image the planner is given), from a representative episode (cell ds3 ss42). Two-Room: traverse a doorway to a goal in the adjacent room; Reacher: move a jointed arm’s end-effector to a target; Push-T: align a T-block to a target pose; OGBench-Cube: push a block to a target. Frames are rendered by the actual simulators (the planner observes 224× 224 pixels).\", \"page\": 8, \"index\": 5, \"width\": 827, \"height\": 221, \"caption_zh\": \"图 5：四个基于像素的目标到达任务。对于每个任务，我们展示两个真实评估帧：起始观测 o0 和基准提供的目标观测 og（规划器获得的目标图像），来自一个代表性 episode（cell ds3 ss42）。Two-Room：穿过门道到达相邻房间中的目标；Reacher：将关节臂的末端执行器移动到目标；Push-T：将 T 形块对齐到目标位姿；OGBench-Cube：将方块推到目标位置。帧由实际模拟器渲染（规划器观测 224× 224 像素）。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-006.webp\", \"caption\": \"Table 2: Fresh-seed 12-cell totals (success rate, %; n=12 cells/task). ∆̄ is the mean cell-level paired delta (IMWM − baseline); 95% CIs are paired-cell bootstrap intervals over the evaluated grid (B=10,000). W/T/L tie band is |∆| < 2 pp; at 50 episodes/cell this reduces to ∆ = 0.\", \"page\": 8, \"index\": 6, \"width\": 799, \"height\": 263, \"caption_zh\": \"表 2：fresh-seed 12-cell 总计（成功率，%；n=12 cells/task）。∆̄ 是平均单元级配对差值（IMWM − baseline）；95% CI 是在评估网格上进行的配对单元 bootstrap 区间（B=10,000）。W/T/L 的平局带为 |∆| < 2 pp；在每个单元 50 个 episode 时，这等价于 ∆ = 0。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-007.webp\", \"caption\": \"Figure 6: Two-feature routing plane (rinv, rlag): each point is one of the 24 diagnostic cells, colored by its routed recipe. Frozen thresholds (Tinv, Tlag) = (0.05, 0.3) are the axis-aligned partition lines; the gate is the evaluated routing mechanism, not an interpolated switch.\", \"page\": 9, \"index\": 7, \"width\": 713, \"height\": 236, \"caption_zh\": \"图 6：双特征路由平面 (rinv, rlag)：每个点代表 24 个诊断单元之一，并按其路由到的配方着色。冻结阈值 (Tinv, Tlag) = (0.05, 0.3) 是轴对齐的划分线；gate 是被评估的路由机制，而不是插值开关。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-008.webp\", \"caption\": \"Figure 8: Per-cell paired success scatter (IMWM vs. the world-model-only baseline), one panel per task. Each point is one of 12 fresh-seed cells; color codes W/T/L under the strict |∆| < 2 pp tie threshold. Generated from the canonical 300/30/30 evaluation data underlying Table 2.\", \"page\": 31, \"index\": 8, \"width\": 689, \"height\": 205, \"caption_zh\": \"图 8：逐单元配对成功率散点图（IMWM vs. 仅世界模型基线），每个任务一个面板。每个点代表 12 个 fresh-seed 单元之一；颜色表示在严格 |∆| < 2 pp 平局阈值下的 W/T/L。由支撑表 2 的规范 300/30/30 评估数据生成。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-009.webp\", \"caption\": \"Table 6: Search-space map of frozen-substrate cost / proposal / retrieval interventions, ordered by intervention layer. All interventions hold the IMWM evaluated recipe constant except at the intervention’s stated layer.\", \"page\": 20, \"index\": 9, \"width\": 822, \"height\": 461, \"caption_zh\": \"表 6：冻结基底上的 cost / proposal / retrieval 干预的搜索空间图，按干预层排序。除干预所声明的层外，所有干预均保持 IMWM 的评估配方不变。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-010.webp\", \"caption\": \"Figure 2: IMWM architecture. Two frozen models encode the start/goal observations and score a candidate action chunk: the Intuition Model (compatibility D = q · k/τ ) and the World Model (a 5-step latent rollout scored by terminal MSE to the goal). Their outputs are combined by the Hybrid Cost J , a weighted sum (weights α, β) of the z-scored negative intuition score (−D) and the z-scored world-model error. Retrieval Initialization seeds the CEM proposal from a demonstration bank, and the Reliability Gate sets the per-cell recipe (retrieval on/off and the weights α, β). For each CEM candidate the Hybrid Cost queries both frozen models; CEM returns the executed action chunk a∗. The start/goal panels are real OGBench-Cube observations.\", \"page\": 5, \"index\": 10, \"width\": 829, \"height\": 590, \"caption_zh\": \"图 2：IMWM 架构。两个冻结模型对起点/目标观测进行编码并为候选动作片段评分：Intuition Model（兼容性 D = q · k/τ）和 World Model（5 步潜在展开，并用终端 MSE 到目标进行评分）。二者的输出由 Hybrid Cost J 组合，即 z-score 后的负直觉分数 (−D) 与 z-score 后的世界模型误差的加权和（权重 α, β）。Retrieval Initialization 从 demonstration bank 为 CEM proposal 播种，Reliability Gate 设置逐单元配方（retrieval 开/关以及权重 α, β）。对于每个 CEM 候选，Hybrid Cost 查询两个冻结模型；CEM 返回被执行的动作片段 a∗。起点/目标面板是真实的 OGBench-Cube 观测。\"}, {\"url\": \"assets/figures/arxiv/2606.01626v1/fig-011.webp\", \"caption\": \"Figure 7: The search bottleneck of Theorem A.1. Left: under a perfect, value-aligned cost, a finite planner’s NT+1 black-box queries are blind (each returns the constant cfail until one lands in the tiny success ball Sε(a†)); the union of their ε-balls covers at most (NT+1) cm εm of the action domain, independent of predictor quality. Right: a proposal centered near a† concentrates mass on Sε and evades the volume term. This is the theoretical proposal-side escape (Appendix A.2, Theorem A.4; one-step / best-so-far CEM under retrieval-coverage assumptions) that motivates the Retrieval Initialization component of §4; we do not claim retrieval initialization is independently responsible for the empirical gains (Appendix G).\", \"page\": 16, \"index\": 11, \"width\": 760, \"height\": 397, \"caption_zh\": \"图 7：Theorem A.1 的搜索瓶颈。左：在完美且与价值对齐的 cost 下，有限规划器的 NT+1 次黑盒查询是盲目的（每次都返回常数 cfail，直到某次落入微小的成功球 Sε(a†)）；这些 ε 球的并集至多覆盖动作域的 (NT+1) cm εm，与预测器质量无关。右：以 a† 附近为中心的 proposal 将质量集中到 Sε 上，从而避开体积项。这是 proposal 侧的理论逃逸机制（附录 A.2，Theorem A.4；在 retrieval-coverage 假设下的一步 / best-so-far CEM），它激励了 §4 的 Retrieval Initialization 组件；我们并不声称 retrieval initialization 独立地导致了经验增益（附录 G）。\"}]"
motivation: 仅提升世界模型精度不足以保证规划成功，有限搜索预算下的动作搜索本身可能成为主要瓶颈。
method: IMWM 用示范训练直觉模型识别有前景动作，并通过检索初始化、混合代价和可靠性门控与世界模型协同规划。
result: 在 Two-Room、Reacher、Push-T 和 OGBench-Cube 四个像素目标到达任务上，IMWM 的平均成功率均优于仅使用世界模型的规划器。
conclusion: 直觉模型能够有效补充世界模型，缓解采样规划中的搜索瓶颈，从而提升潜空间规划的可靠性与成功率。
---

## 摘要
基于学习到的潜在世界模型进行规划，是一种从原始像素实现控制的有前景途径，但仅有强大的世界模型并不足够。我们通过实验表明了这一点：即使在完美世界模型的条件下（通过用真实环境动力学的理想化展开替代学习到的前向预测器来实现），有限预算的基于采样的规划器在某些任务上仍会失败，这表明瓶颈可能在于搜索而非世界模型的精度。受这一差距的启发，我们提出 IMWM（直觉模型 + 世界模型），将世界模型与一个由示范训练而成、用于识别有前景动作的直觉模型相结合。两个模型通过三个轻量级组件协同工作：（i）检索初始化，从检索到的示范中初始化规划器的动作提议；（ii）混合代价，将直觉得分与世界模型展开代价相结合；（iii）可靠性门控，在不同情形下调整规划器对直觉的信任程度。在四个基于像素的目标到达任务（Two-Room、Reacher、Push-T 和 OGBench-Cube）上，IMWM 在所有四项任务中的平均成功率均高于仅使用世界模型的规划器，其中在 Two-Room（99.2%，+11.5 个百分点）和 OGBench-Cube（94.7%，+28.5 个百分点）上提升最大。

## Abstract
Planning with a learned latent world model is a promising route to control from raw pixels, but a strong world model alone is not enough. We show this experimentally: even with a perfect world model (operationalized by replacing the learned forward predictor with an idealized rollout of the true environment dynamics), a finite-budget sample-based planner still fails on some tasks, indicating that the bottleneck can lie in search rather than in world-model accuracy. Motivated by this gap, we propose IMWM (Intuition Model + World Model), which pairs the world model with an intuition model trained from demonstrations to recognize promising actions. The two models collaborate through three lightweight components: (i) Retrieval Initialization, which initializes the planner's action proposal from a retrieved demonstration; (ii) Hybrid Cost, which combines the intuition score with the world-model rollout cost; and (iii) a Reliability Gate, which adjusts how much the planner trusts intuition in each setting. Across four pixel-based goal-reaching tasks (Two-Room, Reacher, Push-T, and OGBench-Cube), IMWM has higher mean success than the world-model-only planner on all four, with the largest gains on Two-Room (99.2%, +11.5 percentage points) and OGBench-Cube (94.7%, +28.5 percentage points).