---
title: "PARE: Pruning and Adaptive Routing for Efficient Video Generation"
title_zh: PARE：用于高效视频生成的剪枝与自适应路由
authors: "Yutong Wang, Yunke Wang, Tianfan Xue, Yu Qiao, Yaohui Wang, Xinyuan Chen, Chang Xu"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27336v1"
tags: ["query:upstream-ad-planning-idea"]
score: 7.0
evidence: 剪枝与自适应路由加速视频扩散
tldr: PARE面向视频扩散Transformer计算开销大的问题，提出结合结构感知剪枝与输入自适应路由的高效视频生成框架：在宽度上区分空间/时间注意力头以保留运动关键信息，在深度上按去噪步和视觉内容动态选择执行模块，并通过渐进式蒸馏训练恢复质量，实现更低计算量下保持生成效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27336v1/fig-001.webp\", \"caption\": \"Table 3: Ablation studies on PARE Training. Each subtable varies one design choice while keeping\", \"page\": 10, \"index\": 1, \"width\": 841, \"height\": 347, \"caption_zh\": \"表 3：PARE 训练的消融实验。每个子表在保持\"}, {\"url\": \"assets/figures/arxiv/2605.27336v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of PARE. (a) Spatial-temporal aware width pruning. For attention, head importance scores si,j are computed on a calibration set and adjusted to s̃i,j based on each head’s spatial-temporal type, protecting motion-critical temporal heads from magnitude bias. For FFN, neurons are ranked by the product of their up/down projection norms and greedily selected while skipping candidates that are functionally similar to already-chosen ones, ensuring both importance and diversity. (b) Content-adaptive block routing. A lightweight router takes a timestep embedding and a visual condition as input and produces a binary mask that selects which pruned blocks to execute. A timestep-dependent budget allocates more blocks at high-noise steps for structure recovery and fewer at low-noise steps for detail refinement.\", \"page\": 3, \"index\": 2, \"width\": 821, \"height\": 306, \"caption_zh\": \"图 1：PARE 概览。(a) 空间-时间感知的宽度剪枝。对于 attention，在校准集上计算 head 重要性分数 si,j，并根据每个 head 的空间-时间类型将其调整为 s̃i,j，从而保护对运动至关重要的 temporal heads 免受幅值偏差影响。对于 FFN，神经元根据其上/下投影范数的乘积进行排序，并在跳过与已选神经元功能相似的候选项的同时贪婪选择，从而同时确保重要性和多样性。(b) 内容自适应的 block routing。轻量级 router 以 timestep embedding 和 visual condition 作为输入，生成一个二值 mask，用于选择要执行的剪枝后 blocks。timestep 相关的预算在高噪声步骤分配更多 blocks 以进行结构恢复，在低噪声步骤分配更少 blocks 以进行细节细化。\"}, {\"url\": \"assets/figures/arxiv/2605.27336v1/fig-003.webp\", \"caption\": \"Table 1: Text-to-video generation results on VBench. We report VBench dimension scores (%).\", \"page\": 8, \"index\": 3, \"width\": 840, \"height\": 506, \"caption_zh\": \"表 1：VBench 上的 text-to-video 生成结果。我们报告 VBench 维度分数（%）。\"}, {\"url\": \"assets/figures/arxiv/2605.27336v1/fig-004.webp\", \"caption\": \"Figure 2: Spatial-temporal head analysis of Wan2.1-14B. (a) Head type distribution per block, classified by intra-slice attention ratio (Eq. (4)). Shallow blocks (3–12) are spatial-dominant, while mid-to-deep blocks (15–31) are temporal-dominant. (b) Block-level average intra-slice attention ratio. The thresholds τs=0.7 and τt=0.3 partition heads into spatial, mixed, and temporal types. This heterogeneity motivates type-aware importance scoring: temporal heads have smaller activations yet are critical for motion coherence, and would be disproportionately pruned by magnitude-only criteria.\", \"page\": 4, \"index\": 4, \"width\": 813, \"height\": 201, \"caption_zh\": \"图 2：Wan2.1-14B 的空间-时间 head 分析。(a) 每个 block 的 head 类型分布，根据 intra-slice attention ratio（Eq. (4)）分类。浅层 blocks（3–12）以 spatial-dominant 为主，而中深层 blocks（15–31）以 temporal-dominant 为主。(b) block 级平均 intra-slice attention ratio。阈值 τs=0.7 和 τt=0.3 将 heads 划分为空间、混合和时间类型。这种异质性促使采用类型感知的重要性评分：temporal heads 的激活更小，但对运动一致性至关重要，若仅采用基于幅值的准则则会被不成比例地剪除。\"}, {\"url\": \"assets/figures/arxiv/2605.27336v1/fig-005.webp\", \"caption\": \"Table 2: Image-to-video generation results on VBench. Same evaluation protocol as Table 1.\", \"page\": 9, \"index\": 5, \"width\": 844, \"height\": 477, \"caption_zh\": \"表 2：VBench 上的 image-to-video 生成结果。评估协议与表 1 相同。\"}, {\"url\": \"assets/figures/arxiv/2605.27336v1/fig-006.webp\", \"caption\": \"Figure 4: Qualitative comparison between the teacher, PARE, and PARE + Step Distill.\", \"page\": 9, \"index\": 6, \"width\": 829, \"height\": 208, \"caption_zh\": \"图 4：teacher、PARE 和 PARE + Step Distill 的定性比较。\"}, {\"url\": \"assets/figures/arxiv/2605.27336v1/fig-007.webp\", \"caption\": \"Figure 3: From block importance to learned routing. (a) Per-block contribution of the teacher model across denoising timesteps, measured by the residual norm ∥Bi(x(i))− x(i)∥2 (each block normalized to [0, 1] independently for visualization). Larger residual norms indicate that a block modifies the hidden state more at that timestep. The pattern reveals substantial heterogeneity: early blocks and boundary blocks contribute consistently, while middle blocks show timestep-dependent variation, suggesting that adaptive block selection can exploit this structure. (b) Activation frequency of the learned router. The router recovers the teacher’s importance structure, selectively skipping blocks deemed unimportant by the teacher, while enforcing a timestep-dependent budget that allocates more blocks to high-noise steps (structure recovery) and fewer to low-noise steps (detail refinement).\", \"page\": 5, \"index\": 7, \"width\": 811, \"height\": 181, \"caption_zh\": \"图 3：从 block 重要性到学习式 routing。(a) teacher model 在各去噪 timestep 上的逐 block 贡献，通过残差范数 ∥Bi(x(i))− x(i)∥2 衡量（为便于可视化，每个 block 独立归一化到 [0, 1]）。较大的残差范数表明该 block 在该 timestep 对 hidden state 的修改更大。该模式揭示了显著的异质性：早期 blocks 和边界 blocks 始终有贡献，而中间 blocks 表现出 timestep 相关的变化，表明自适应 block 选择可以利用这一结构。(b) 学习到的 router 的激活频率。router 恢复了 teacher 的重要性结构，选择性跳过 teacher 判定为不重要的 blocks，同时施加 timestep 相关的预算，在高噪声步骤分配更多 blocks（结构恢复），在低噪声步骤分配更少 blocks（细节细化）。\"}]"
motivation: 现有视频DiT加速方法多采用固定压缩结构，难以适应不同输入和去噪阶段的计算需求。
method: PARE通过空间/时间注意力头感知的宽度剪枝和基于时间步与内容的轻量路由器进行动态深度选择，并采用渐进式蒸馏联合优化。
result: 在Wan2.1-14B的图生视频和文生视频任务上，PARE显著降低单步计算量，同时在VBench多项指标上保持生成质量。
conclusion: PARE证明了宽度剪枝与输入自适应深度路由结合可有效提升视频生成效率，并可与步数蒸馏进一步叠加加速。
---

## 摘要
视频扩散 Transformer（DiT）能够生成高质量视频，但由于其宽模块、深层架构以及迭代式采样，需要大量计算资源。近期方法通过压缩宽度、深度或采样步数来降低成本，但通常依赖固定架构，无法适应单个输入或不同去噪阶段。我们提出 PARE（Pruning and Adaptive Routing for Efficient video generation），通过结构感知剪枝与输入自适应路由联合压缩宽度和深度。对于宽度压缩，我们观察到注意力头会专门分化为空间和时间角色，并设计了考虑这一区分的重要性评分，以防止对运动至关重要的时间注意力头被过早剪枝。对于深度压缩，我们训练了一个轻量级路由器，其以去噪时间步和视觉内容为条件，在每一步动态选择要执行的模块，从而实现按输入自适应的计算，而非静态移除模块。一个渐进式流程首先通过蒸馏恢复宽度剪枝后的质量，随后联合优化学生模型与路由器，以解耦这两个学习目标。在 Wan2.1-14B 上针对图像到视频和文本到视频生成进行的实验表明，PARE 在保持 VBench 各维度质量的同时显著降低了每步计算量，并且可与步数蒸馏结合以实现进一步加速。

## Abstract
Video Diffusion Transformers (DiTs) generate high-quality videos but demand substantial compute due to wide blocks, deep architectures, and iterative sampling. Recent methods reduce cost by compressing width, depth, or sampling steps, but typically commit to a fixed architecture that cannot adapt to individual inputs or denoising stages. We propose PARE (Pruning and Adaptive Routing for Efficient video generation), which jointly compresses width and depth with structure-aware pruning and input-adaptive routing. For width, we observe that attention heads specialize into spatial and temporal roles, and design importance scoring that accounts for this distinction to prevent motion-critical temporal heads from being pruned prematurely. For depth, we train a lightweight router conditioned on denoising timestep and visual content to dynamically select which blocks to execute at each step, enabling per-input compute adaptation rather than static block removal. A progressive pipeline first recovers width-pruned quality via distillation, then jointly optimizes the student and router to decouple the two learning objectives. Experiments on Wan2.1-14B for both image-to-video and text-to-video generation show that PARE substantially reduces per-step computation while preserving quality across VBench dimensions, and composes with step distillation for further acceleration.