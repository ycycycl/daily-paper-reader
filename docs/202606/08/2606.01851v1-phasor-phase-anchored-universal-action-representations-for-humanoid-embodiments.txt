Title: PHASOR: Phase-Anchored Universal Action Representations for Humanoid Embodiments

URL Source: https://arxiv.org/pdf/2606.01851v1

Published Time: Tue, 02 Jun 2026 02:57:48 GMT

Number of Pages: 19

Markdown Content:
# PHASOR: Phase-Anchored Universal Action Representations for Humanoid Embodiments 

Kihyun Kim 1,2∗ Chaeyun Kim 1,2∗† Jongho Shin 3 Taeyoun Kwon 2,4

Junghyun Kim 2,5 Mijin Koo 1,2 Haon Park 1,2†

> 1

AIM Intelligence 2Seoul National University 3LG Electronics 

> 4

MAUM AI 5OpenMind 

Abstract: Learning a good action embedding space is fundamental to scalable robot policy learning, yet existing methods treat action latents as task-specific intermediates rather than first-class representations. The resulting latents are un-structured, embodiment-specific, and weakly tied to motion semantics, limiting interpretability, controllability, and transferability across robots. We position the action embedding space itself as a first-class design target, with downstream pol-icy quality emerging from representation quality. Exploiting motion’s intrinsic periodicity, we factorize it into a phase manifold that captures cyclic structure via FFT-parametric coefficients, together with a pose branch that conditions the mani-fold on non-periodic configuration detail. Combined with motion-semantic distil-lation, this factorized structure yields a cross-embodiment motion manifold that is interpretable and embodiment-agnostic by design. Anchoring multiple humanoid robots to a shared human-pretrained manifold then produces a unified action em-bedding space across diverse platforms, achieving strong cross-embodiment re-trieval and consistent gains on downstream robot tasks. 

Keywords: Action Latent, Cross-Embodiment, Phase Manifold, Humanoid 

## 1 Introduction 

The rapid diversification of humanoid robot platforms has created a pressing need for action repre-sentations that generalize across embodiments. Modern robot policies, from vision-language-action models [1, 2, 3] to diffusion-based controllers [4, 5], all build on learned action spaces as the inter-mediate signal between perception and motor execution. In cross-embodiment settings, this space becomes the primary bridge through which knowledge transfers from one body to another, making its quality a bottleneck for generalization. Yet current action encoders [6, 7, 8, 9, 10] treat motion as a generic sequence of per-frame joint states, with no motion-specific structure. This leads to two failures. First, the representation entan-gles with the morphology of the training robot, so the same behavior on a different humanoid maps to a disjoint region and cannot transfer. Second, the latent space lacks temporal organization, and proximity within it does not reflect proximity in time. Thus its dimensions carry no interpretable meaning, leaving trajectories hard to analyze or control. To overcome these limitations, we exploit the intrinsic physical structure of motion. Unlike text or images, motion is fundamentally a superposition of periodic components. During walking, the legs oscillate at roughly 1 Hz, the arms swing in antiphase, and the trunk rotates more slowly. Decompos-ing motion into amplitude, frequency, and phase shift [11] separates two quantities that unstructured encoders conflate. Phase encodes where in the action the body is, while amplitude and frequency encode what action is performed. We hypothesize that this periodic structure is intrinsic to the behavior rather than to the body that produces it, and can therefore serve as a shared substrate for aligning action embeddings across robots [12]. 

> ∗

Equal contribution. †Corresponding author. 

> arXiv:2606.01851v1 [cs.RO] 1 Jun 2026

Realizing this phase manifold as a cross-embodiment action space requires solving two problems. First, existing approaches [11, 12, 13, 14] compress the whole body into a single representation, blurring the distinct periodicities of individual joints. We instead decompose the phase encoding by body part, preserving each limb’s periodic signature as a natural unit of correspondence across kinematics. Second, encoding motion through velocity alone captures how the body moves but not where it is; so motions with similar oscillation but different poses (e.g., upright vs. crouched walking) collapse to nearby points. We resolve this with a separate pose stream that supplies non-periodic context to the decoder, leaving P(τ ) uncontaminated. With this clean manifold established, the remaining challenge is extending it across embodiments. Since periodic structure belongs to the behavior rather than the skeleton, and human data far out-numbers fragmented robot data, we anchor the shared manifold in human motion. Here, each new robot joins through a lightweight adapter rather than relearning the manifold from scratch. We propose PHASOR (PH ase Action Space for cross-emb Odiment Representation), a framework built on the periodic structure of motion. A per-body-part phase encoder extracts periodic parame-ters from joint velocities to assemble a phase manifold P(τ ), while a complementary pose branch supplies non-periodic context through FiLM [15] without leaking into P(τ ). The human-pretrained encoder acts as a shared anchor, and each robot joins through a lightweight adapter. An alignment head then projects per-embodiment manifolds onto a unit sphere and pulls semantically equivalent motions together, guided by a pretrained semantic motion prior [16]. We validate this design on one human and four humanoid robots along two axes, embedding qual-ity and downstream utility . The phase manifold yields a tightly aligned cross-embodiment space, reaching over 88% cross-embodiment retrieval accuracy and surpassing unstructured and quantized baselines. As a learned representation, it further improves motion imitation, teleoperation, and rein-forcement learning, confirming that motion-intrinsic periodicity is an effective design prior. Our contributions are: 1. We propose the phase manifold as a design prior for cross-embodiment action embedding, where per-body-part parameters yield an interpretable and controllable action space. 2. We introduce a human-anchored multi-embodiment alignment strategy that extends a frozen phase oracle to four robots through lightweight adapters. 3. Our structured embedding delivers downstream gains in motion imitation and teleoperation, with strong cross-embodiment retrieval .

## 2 Related Works 

Latent Action Representations for Robot Policy. Modern robot policies learn an action repre-sentation that mediates between observation and motor command, formulating this space through autoregressive tokenization [1, 10, 8, 3], diffusion and flow-matching denoising [4, 2], or structured latents from video reconstruction [6, 9, 17] and discrete codebooks [7, 18, 5]. These treat motion as an unstructured sequence, overlooking the periodic, morphology-invariant nature of humanoid movement. We instead build the action embedding around the periodic structure of motion. 

Periodic Representations in Motion. Phase compactly describes where a behavior lies within a pe-riodic cycle. Early methods modulated network weights from foot-contact heuristics [19], later ex-tended to multi-contact and limb-specific phases [20, 21, 22], and unsupervised approaches learned multi-dimensional phase manifolds via FFT bottlenecks [11], extending to style modulation and in-betweening [23, 24]. Recent work applies phase to human-quadruped alignment [12], functional autoencoders [14], per-body-part control [25], and periodic skill discovery [26]. Yet all remain confined to character animation, never used as action representations for humanoid robots or as a substrate for cross-embodiment alignment. 

Humanoid Cross-Embodiment Transfer. Humanoid transfer can be grouped by alignment mech-anism. Kinematic retargeting maps reference motion to a target body via inverse kinematics or interaction-aware optimization, yielding training data but no shared representation [27, 28, 29, 30, 2Walk :  

> Upright / Leg-Dominant
> Bend down :
> In-Place / Forward bend
> Wave :
> Upright / Arm-Dominant
> (a) Whole-body (A, F) profile
> (b) Per body-part amplitude (c) Phase parameters for Walk vs. Bend

Figure 1: Structural gaps of PAE. (a,b) Whole-body (A, F ) conflates distinct motions (walk vs. wave), which per-part features resolve. (c) Velocity-based (A, F ) ignores posture difference. 31]. Shared-latent approaches instead project motions into a common space via contrastive learning on extrinsic kinematics [32, 33], scaled by per-part embeddings [34], or unified through differen-tiable retargeting [35], visual transfer [36, 37, 38], and language-guided features [39, 40, 41]. All rest on per-frame similarity over extrinsic or task-specific features, with no motion-intrinsic struc-ture. We instead align heterogeneous robots on a structured, physically meaningful phase manifold. 

## 3 Preliminary: Periodic Motion Decomposition 

Periodic Autoencoder (PAE). Given a motion window X ∈ RD×T , a 1D convolutional encoder g

produces M latent channels L = g(X) ∈ RM ×T , and an FFT-based bottleneck fits each channel to a single sinusoid with amplitude Ac, frequency Fc, phase shift Sc, and offset Bc, trained end-to-end under a reconstruction loss so that each channel captures one locally periodic component. 

ˆlc(τ ) = Ac cos  2π(Fcτ + Sc) + Bc (1) 

Phase Manifold. Each channel is projected onto an amplitude-scaled circle and stacked over chan-nels to form a phase manifold P(τ ) = [ m1(τ ); . . . ; mM (τ )] ∈ R2M

ϕc(τ ) = 2 π(τ F c + Sc), mc(τ ) = Ac

 cos ϕc(τ ), sin ϕc(τ ) (2) a counter-clockwise trajectory on a product of circles. Here (Ac, F c) encodes action identity (e.g., walking vs. running) while Sc encodes temporal position within action, so points close on P are both semantically similar and temporally aligned [11]. 

Limitations for Cross-Embodiment Encoding. Applied to the cross-embodiment setting, this formulation has two structural gaps (Fig. 1). First, the encoder compresses the whole body into M

shared channels, so parts moving at independent rhythms (e.g., arms waving while walking) collapse into a weighted mixture (Fig. 1.(a)), erasing the part-level periodicity that is the natural unit of cross-embodiment correspondence (Fig. 1.(b)). Second, the encoding is velocity-based: it captures how the body moves but not where it is, so motions with similar (A, F ) but different posture map to nearby points (Fig. 1.(c)). We mitigate both gaps: per-body-part encoding recovers part-level periodicity, and auxiliary pose branch supplies static configuration without contaminating P(τ ).

## 4 Method 

We propose PHASOR , which learns a structured action embedding across one human and K hu-manoid robots using periodic motion structure as its design prior. As shown in Fig. 2, motion is decomposed into a phase manifold P(τ ) that captures periodic dynamics and a complementary pose branch that captures non-periodic configuration. P(τ ) is the shared coordinate system for cross-embodiment alignment, while the pose branch stays per-embodiment. We first build this embedding for a single embodiment (Sec. 4.1), then extend it across embodiments via a human-anchored scheme (Sec. 4.2), training in two stages: establish the human oracle, then align robots on top of it. 3(a) Architecture Overview (b) Alignment Loss Detail                      

> π
> L_pair: Soft InfoNCE
> Phase Manifold
> Pose Branch (auxiliary)
> +
> π Alignment Head representation hard labels (70%) soft similarity (30%)
> [TRAINABLE]
> L_speed: Traversal speed matching
> Shared Robot Decoder (Dx) MAE of per-frame step sizes
> Human
> decoder L_shape : Orbit shape matching
> L_align :
> paired targets closer to
> human anchor multi embodiment shared
> half-period chord distances match
> Frozen (pretrained) Trainable (robot-specific) Shared oracle Pose (auxiliary) Frozen weights
> V_joint (Human) V_joint 1 V_joint 2 V_joint 3 V_joint 4
> Shared Phase Encoder [FROZEN]
> 5x Conv1D + FFT →(A, F, B, S), M=16
> LL RL LA RA TK
> Pose Encoder
> Pose Decoder
> per-emb
> input
> pose/detail
> vector
> (weak) condition
> FiLM
> Phase ↔Pose
> L_Phase

Figure 2: Overview of PHASOR. (a) A frozen human phase encoder, shared across embodi-ments via per-robot adapters, yields a morphology-invariant manifold P(τ ); a per-embodiment pose branch couples in only at the decoder via bidirectional FiLM. (b) Manifolds are projected onto S63 

and aligned by soft InfoNCE (LAMP prior) plus speed and shape consistency terms. 

4.1 From Periodic Prior to Structured Action Manifold Task Definition. We consider one human ( JH =22 SMPL-H joints) and K robots with joint counts 

Jk. Each motion is a root-local window of T =121 frames ( ≈2 s, 60 fps), with joint velocity 

vjoint ∈ RT ×3J , 6D joint rotation Rjoint ∈ RT ×6J , and root position Xroot ∈ RT ×3, on normalized time τ ∈ [− T

2 fps , T

2 fps ]. Our goal is a phase manifold P(τ ) ∈ RT ×2M , paired with a complemen-tary pose code for non-periodic posture, such that semantically equivalent windows from different embodiments map to nearby trajectories on a shared coordinate system while staying interpretable and controllable per embodiment. 

4.1.1 Per-Part Periodic Decomposition 

A whole-body encoder conflates joints with independent rhythms (Sec. 3). We instead partition every embodiment into five kinematic parts B = {LA , RA , TK , LL , RL } (left/right arm, trunk, left-/right leg), each with its own phase encoder Φb and periodic bottleneck; per-embodiment joint-to-part assignments follow kinematic role (Sec. A.1). For each part b, Φb models the velocity signal 

vb(τ ) as Kb sinusoids, extracting (Ab,c , F b,c , B b,c ) via a differentiable FFT layer and Sb,c via a regression head [11]. Channels are allocated by each part’s rhythmic complexity, M = 16 in total. 

vb(τ ) ≈

KbX

c=1 

Ab,c cos  2π(Fb,c τ + Sb,c ) + Bb,c , (3) We then project every channel onto its amplitude-scaled circle via Eq. (2), now indexed by part as 

mb,c (τ ) = Ab,c [cos ϕb,c (τ ), sin ϕb,c (τ )] , and concatenate over all (b, c ) to form the part-structured manifold P(τ ) ∈ RT ×32 ; a symmetric per-part decoder Db reconstructs Eq. (3). Independent encoders Φb keep each part’s rhythmic signature free of cross-part contamination. 

4.1.2 Complementary Pose Conditioning 

Velocity-based phase lacks static postural context (e.g., crouching vs. standing), so we add a com-plementary pose branch that encodes 6D joint rotations and root positions into a compact token 4sequence zpose ∈ RLp×dp (Lp=8 , dp=128 ), retaining only window-level postural configuration. The two branches interact exclusively at decoding via bidirectional FiLM [15]: phase context con-ditions pose decoding, while pose context modulates the reconstructed velocity signal before the phase decoder. Crucially, P(τ ) is constructed from FFT parameters before any modulation, keeping the manifold pose-free and suitable as a clean cross-embodiment alignment target (Sec. 4.2). 

4.2 Cross-Embodiment Alignment on the Phase Manifold 

As the channels of P(τ ) carry rhythmic identity rather than morphology, we treat it as a shared coordinate system. Every robot is mapped onto the same manifold and aligned by semantic corre-spondence. We realize this by reusing the human-pretrained backbone as a frozen oracle and routing each robot through it with lightweight adapters. 

4.2.1 Human-Anchored Encoder 

Human data is abundant and behaviorally diverse while robot data is sparse, so we pretrain the backbone on human motion and freeze it as a shared anchor where every robot is aligned to. The per-part encoders Eh = {Φb}b∈B and the phase-to-pose FiLM head are frozen, fixing the channel semantics once so new robots join without representation drift. For robot k, a linear input adapter 

Ekr : RDk → R66 maps its joint velocities into the human input space, and the shared frozen encoder 

EX ≡ Eh extracts the phase parameters: 

vjoint 

> Rk
> Ekr

−−→ ˜v(66)  

> Rk
> EX≡Eh

−−−−−→ (Ab,c , F b,c , B b,c , S b,c ) Eq. 3 

−−−→ PRk (τ ). (4) Robot joints use the same five-part partition (Sec. A.1). Since EX is shared, all morphological variation is absorbed by the small adapter Ekr , and PRk (τ ) adheres to the human phase prior. 

4.2.2 Decoder Adaptation and Pose Isolation 

The decoder mirrors the encoder but allows controlled adaptation: the human phase decoder Dh

stays frozen, while robots share a phase decoder DX initialized from Dh and fine-tuned with per-robot output adapters Dkr : R66 → RDk . Because posture is morphology-dependent, each robot has its own pose branch supervised by reconstruction only, keeping morphology-specific detail out of the shared-manifold alignment. 

4.2.3 Alignment Objective 

All supervision acts on P(τ ), not on the pose branch, keeping the signal independent of kinematics. 

Alignment head. For latent alignment, we concatenate the phase manifold P(τ ) with a temporally pooled pose token zpool pose and project the result onto a unit sphere via a lightweight head π.

z(τ ) = π [ P(τ ) ; zpool pose ] ∈ S 63 . (5) The pose token is added to disambiguate motions that share the same rhythm but hold a different posture, which phase alone cannot tell apart. To keep alignment driven mainly by phase, the pose-related weights in π are initialized near zero, so the head starts phase-only and brings in pose context gradually. Time-pooling z(τ ) over a clip gives a clip-level descriptor zpool ∈ S 63 .

Hierarchical Pair Loss. For each human-robot pair we use an InfoNCE objective whose target mixes the hard diagonal label with a soft target from a motion-semantic prior (LAMP [16]). 

T mix = (1 − λmix )T hard + λmix T soft LAMP (6) The hard term anchors paired windows, while the LAMP term pulls together semantically similar motions that hard labels treat as negatives. We apply T mix at two granularities, per-window for 

fine motion variation and per-clip (mean-pooled z within a clip) for coarse meaning, summing the symmetric cross-entropy of both into Lpair .5H→R R→H R→R                                                                                                                                   

> Method R@1 R@5 R@10 MRR R@1 R@5 R@10 MRR R@1 R@5 R@10 MRR MLP [34, 32] 79.9 96.5 98.8 87.1 79.8 97.5 99.3 87.4 74.7 95.0 98.2 83.5 VQ [6, 5] 37.6 78.6 91.0 55.1 36.3 77.0 89.5 53.6 37.5 77.7 89.8 54.8
> (A) Alignment loss design
> Only Phase (zero-shot) 83.0 91.9 93.6 87.0 83.9 92.2 94.1 87.8 73.0 84.3 87.3 78.2 Hard only (no LAMP) 84.1 92.2 93.8 87.8 84.1 92.5 94.2 87.8 74.5 85.3 88.1 79.5
> +Soft Coarse 84.9 92.7 94.3 88.5 85.1 93.0 94.6 88.6 76.1 86.4 88.9 80.7
> +Soft Fine 85.6 93.4 94.9 89.1 85.7 93.7 95.2 89.1 77.2 87.1 88.6 81.6
> +Soft Coarse +Fine 86.9 94.3 95.8 89.9 87.2 94.8 96.3 90.2 79.3 89.6 91.8 83.7
> (B) Alignment target (loss =Soft Coarse +Fine)
> π(Pτ∥Xroot )88.5 96.0 97.2 91.9 89.5 96.5 97.5 92.6 81.0 91.7 93.9 85.8
> π(Pτ∥zpose )(Ours) 90.3 98.3 99.2 93.9 90.5 98.6 99.4 94.1 84.8 96.4 98.3 90.0

Table 1: Cross-embodiment retrieval and PHASOR ablation. Each query has one positive, the temporally identical chunk from another embodiment (R →R over  42

=6 pairs, %). (A) varies the loss at a fixed target, adding LAMP soft targets at the coarse clip, fine window, and both levels. (B) 

varies the head input at the best loss, Xroot vs. our pooled zpose . Bold best, underline second. 

Trajectory Consistency. Two lightweight geometric penalties regularize the temporal shape of 

z(τ ): Lspeed matches per-frame step size on the sphere (implicit frequency synchronization) and 

Lshape matches half-period distance (implicit amplitude synchronization). The full alignment loss is 

Lalign = P

> k

 L(k) 

> pair

+ λspeed L(k) 

> speed

+ λshape L(k)

> shape

.

4.2.4 Training 

We optimize the framework in two continuous phases. We first pretrain the human backbone with a per-part velocity reconstruction loss Lphase , a pose reconstruction loss Lpose , and a forward-kinematics consistency loss LFK that geometrically links the two branches. We then freeze the human oracle and jointly train the robot components (input/output adapters, shared phase decoder 

DX , per-robot pose branches, and alignment head π) under Eq. (7). 

Ltotal = λphase Lphase + λpose Lpose + λFK LFK + Lalign (7) A short reconstruction-only warm-up precedes alignment, so each PRk stabilizes before contrastive supervision begins. Full details are provided in Sec. A.6. 

## 5 Experiments 

We evaluate PHASOR along two axes. Embedding quality asks whether the phase manifold yields an interpretable, cross-embodiment-aligned action space (Sec. 5.1), and downstream utility asks whether that embedding produces concrete gains once frozen and plugged into robot policy learning (Sec. B.3). In every downstream evaluation the phase encoder stays frozen and only a lightweight policy head is trained, so any performance gap comes from representation quality alone. 

Data. We use 12,616 clips ( ∼40.4 h, 8.7 M frames at 60 fps) from AMASS CMU and KIT, each retargeted with GMR to four humanoids, Unitree G1 (32 DOF), H1.2 (28 DOF), Booster T1 (24 DOF), and Berkeley Humanoid Lite (25 DOF), yielding 50,464 synchronized human-robot pairs. Every experiment uses a 121-frame chunk ( ∼2 s). 

Baselines. We compare two cross-embodiment embedding strategies. MLP [34, 32] projects per-embodiment features into 80D with a segment-wise MLP under InfoNCE, a contrastive baseline with no structural prior. VQ [6, 5] encodes motion into a 10D discrete codebook via cross-reconstruction, isolating discrete quantization against our periodic prior. As objectives vary across quantizer-based methods, we use a representative codebook variant so the comparison reflects quantization itself. 6Cross embodiment alignment Motion Type Seperation (G1) 

> (Unit Sphere) (Phase Manifold)

Figure 3: Left. 2D PCA of the alignment embedding z (top) and phase manifold P(τ ) (bottom). Same clip across the human and four robots overlaps tightly (col. 1), while G1 separates four motion types (walk, run, jump, kick) by orbit radius and position (col. 2). Right. Phase shifts ∆S on a running clip yield predictable temporal offsets, each row at a different gait phase. 

5.1 Embedding Quality 5.1.1 Cross-Embodiment Retrieval 

We measure alignment with chunk retrieval. From 100 held-out clips we sample 20 non-overlapping 121-frame chunks each and query the gallery of a different embodiment, where each query has one positive, the temporally identical chunk. We report Recall@ k and Mean Reciprocal Rank (MRR) over three directions, Human →Robot (H →R), Robot →Human (R →H), and Robot →Robot (R →R, averaged over all  42

=6 pairs). 

Baselines. MLP reaches reasonable exact-match scores since its objective directly optimizes per-frame discrimination, whereas VQ performs far worse, as its codebook maps adjacent chunks to the same code and collapses the fine-grained distinctions retrieval needs. 

(A) Alignment loss. Applied to robots without cross-embodiment training, the frozen human-only manifold ( Only Phase ) already beats MLP on H →R (83.0 vs. 79.9 R@1), confirming a strong morphology-invariant temporal prior. Cross-embodiment InfoNCE ( Hard only ) improves every di-rection, and LAMP soft targets help further, where coarse clip-level ( Soft Coarse ) and fine window-level ( Soft Fine ) targets each raise accuracy and their combination ( Soft Coarse +Fine ) is best in (A) at 86.9 R@1. The R →R gain is most telling, as robot pairs are never directly aligned in training. Alignment emerges transitively through the shared human anchor, which the LAMP prior, common to all embodiments, makes possible. 

(B) Alignment target. With the loss fixed to Soft Coarse +Fine, we change what the head receives alongside P(τ ). Adding the raw root position Xroot lifts R@1 to 88.5 on H →R and our pooled, FiLM-modulated pose token zpose (Ours ) is best on every metric and direction (90.3 R@1, 93.9 MRR on H →R). The modulated token adds postural context already consistent with the phase manifold, separating same-rhythm motions without injecting raw morphology. 

5.1.2 Manifold Visualization and Interpretability 

The PCA projections (left of Fig. 3) reveal two properties. Intra-clip continuity (second column), within a clip the advancing phase ϕc(τ ) traces a smooth counter-clockwise orbit whose radius reflects amplitude, a signature unstructured embeddings lack. Cross-embodiment overlap (first column), matched timestamps from different embodiments cluster tightly, validating the human-anchored alignment. The alignment embedding z (top) is mildly distorted relative to the raw mani-fold P(τ ) (bottom) yet keeps the circular structure and still separates motion types. We further show that the phase shift Sc is an interpretable, controllable temporal coordinate (right of Fig. 3). For a G1 running clip we read per-window Sc with the frozen encoder, unwrap it into 7Imitation Teleoperation Variant dz MPJPE ↓ dz MPJPE ↓                

> Propr. raw –1.83 –64.85 MLP 80 1.83 80 66.79 VQ 10 1.69 10 65.17
> PHASOR 64 1.62 64 64.75

Figure 4: Left. Downstream results, all encoders frozen and only the policy head trained (MPJPE in mm, ↓). Propr. raw uses raw proprioceptive input with no embedding. Right. IsaacSim walking-forward rollout, left to right PHASOR (ours), VQ, MLP. a monotone timeline, and retrieve windows at evenly spaced offsets Sq + ∆ S. The retrieved start point advances in proportion to ∆S, confirming the embedding captures motion timing and supports per-embodiment phase control. 

5.2 Downstream Tasks 

We test the frozen embedding on three tasks, each freezing the encoder and training only a lightweight policy head to isolate representation quality. As a floor, we add a Proprioceptive raw 

baseline that drives the policy head from raw proprioceptive input alone, with no learned embedding (first row of Fig. 4). The exact proprioceptive inputs and downstream settings are in Sec. B.4. 

Motion Imitation and Teleoperation. Both predict 3D joint positions from a frozen embedding via a 3-layer MLP. In imitation , the G1’s proprioceptive state and z predict its next-frame pose, where PHASOR scores best (Fig. 4) as its explicit phase parameterization pins down the gait-cycle position while the VQ codebook captures periodicity only implicitly. In teleoperation , a human window maps to the matching G1 pose from z alone, where only PHASOR beats the raw-kinematics baseline and MLP falls below it, showing an unstructured projection can discard useful information. 

RL Locomotion with Phase Reward. Here the embedding shapes the reward rather than the policy input. The same frozen encoder f embeds the robot’s online motion and the human reference, and the reward scores their alignment, 

rphase = λphase 

  cos( f (xrobot ), f (xref )) + 1 /2, (8) so each method measures phase agreement in its own space without prescribing a joint-level solu-tion. The reference enters only through the reward and all variants share identical settings, isolating embedding geometry. In Fig. 4, PHASOR walks forward steadily on both feet, VQ under-uses one leg and drifts in a circle, and MLP fails to locomote, confirming our phase embedding gives an effective reward for periodic, walking-like motion. 

## 6 Conclusion 

We present PHASOR, an action representation that treats the periodic structure of motion as a design prior rather than learning the latent space from reconstruction alone. By decomposing motion into per-body-part phase parameters, PHASOR builds a compact, morphology-invariant manifold an-chored in human motion, where each new robot joins through a lightweight adapter against a frozen oracle, yielding an interpretable and controllable action space whose coordinates expose behavior timing. Across one human and four humanoids, this frozen manifold transfers effectively to re-trieval and downstream policy learning, indicating that motion-intrinsic periodicity is a transferable substrate for cross-embodiment action embeddings. 

Limitations. PHASOR is built around periodic structure, so it is most informative for rhythmic, locomotion-style behaviors while providing weaker structure for transient or aperiodic motions, and its pose branch remains per-embodiment, leaving non-periodic posture unshared across bodies. Validation is currently limited to simulation, with physical hardware left to future work. We view these as natural next steps rather than fundamental barriers. 8References 

[1] B. Zitkovich, T. Yu, S. Xu, P. Xu, T. Xiao, F. Xia, J. Wu, P. Wohlhart, S. Welker, A. Wahid, et al. Rt-2: Vision-language-action models transfer web knowledge to robotic control. In 

Conference on Robot Learning , 2023. [2] K. Black, N. Brown, D. Driess, A. Esmail, M. Equi, C. Finn, N. Fusai, L. Groom, K. Hausman, B. Ichter, et al. π0: A vision-language-action flow model for general robot control. arXiv preprint arXiv:2410.24164 , 2024. [3] J. Bjorck, F. Casta˜ neda, N. Cherniadev, X. Da, R. Ding, L. Fan, Y. Fang, D. Fox, F. Hu, S. Huang, et al. Gr00t n1: An open foundation model for generalist humanoid robots. arXiv preprint arXiv:2503.14734 , 2025. [4] C. Chi, Z. Xu, S. Feng, E. Cousineau, Y. Du, B. Burchfiel, R. Tedrake, and S. Song. Diffusion policy: Visuomotor policy learning via action diffusion. The International Journal of Robotics Research , 2025. [5] Z. Luo, Y. Yuan, T. Wang, C. Li, S. Chen, F. Castaneda, Z.-A. Cao, J. Li, D. Minor, Q. Ben, et al. Sonic: Supersizing motion tracking for natural humanoid whole-body control. arXiv preprint arXiv:2511.07820 , 2025. [6] S. Ye, J. Jang, B. Jeon, S. J. Joo, J. Yang, B. Peng, A. Mandlekar, R. Tan, Y.-W. Chao, B. Y. Lin, et al. Latent action pretraining from videos. In International Conference on Learning Representations , volume 2025, pages 28213–28239, 2025. [7] A. Mete, H. Xue, A. Wilcox, Y. Chen, and A. Garg. Quest: Self-supervised skill abstractions for learning continuous control. Advances in Neural Information Processing Systems , 37: 4062–4089, 2024. [8] O. M. Team, D. Ghosh, H. Walke, K. Pertsch, K. Black, O. Mees, S. Dasari, J. Hejna, T. Kreiman, C. Xu, et al. Octo: An open-source generalist robot policy. arXiv preprint arXiv:2405.12213 , 2024. [9] Q. Bu, Y. Yang, J. Cai, S. Gao, G. Ren, M. Yao, P. Luo, and H. Li. Univla: Learning to act anywhere with task-centric latent actions. arXiv preprint arXiv:2505.06111 , 2025. [10] M. J. Kim, K. Pertsch, S. Karamcheti, T. Xiao, A. Balakrishna, S. Nair, R. Rafailov, E. P. Foster, P. R. Sanketi, Q. Vuong, et al. Openvla: An open-source vision-language-action model. In Conference on Robot Learning , 2025. [11] S. Starke, I. Mason, and T. Komura. Deepphase: Periodic autoencoders for learning motion phase manifolds. ACM Transactions on Graphics (ToG) , 41(4):1–13, 2022. [12] P. Li, S. Starke, Y. Ye, and O. Sorkine-Hornung. Walkthedog: Cross-morphology motion alignment via phase manifolds. In ACM SIGGRAPH 2024 Conference Papers , pages 1–10, 2024. [13] B. Ji, Y. Pan, Z. Liu, S. Tan, X. Jin, and X. Yang. Pomp: Physics-consistent motion gen-erative model through phase manifolds. In Proceedings of the Computer Vision and Pattern Recognition Conference , pages 22690–22701, 2025. [14] M. Pegoraro, E. Atherton, B. Roy, A. Khani, and A. Rampini. Funphase: A periodic functional autoencoder for motion generation via phase manifolds. arXiv preprint arXiv:2512.09423 ,2025. [15] E. Perez, F. Strub, H. De Vries, V. Dumoulin, and A. Courville. Film: Visual reasoning with a general conditioning layer. In Proceedings of the AAAI conference on artificial intelligence ,volume 32, 2018. 9[16] Z. Li, W. Yuan, Y. He, L. Qiu, S. Zhu, X. Gu, W. Shen, Y. Dong, Z. Dong, and L. Yang. Lamp: Language-motion pretraining for motion generation, retrieval, and captioning. In International Conference on Learning Representations , volume 2025, pages 84238–84250, 2025. [17] Q. Bu, J. Cai, L. Chen, X. Cui, Y. Ding, S. Feng, S. Gao, X. He, X. Hu, X. Huang, et al. Agibot world colosseo: A large-scale manipulation platform for scalable and intelligent embodied systems. arXiv preprint arXiv:2503.06669 , 2025. [18] S. Lee, Y. Wang, H. Etukuru, H. J. Kim, N. M. M. Shafiullah, and L. Pinto. Behavior generation with latent actions. arXiv preprint arXiv:2403.03181 , 2024. [19] D. Holden, T. Komura, and J. Saito. Phase-functioned neural networks for character control. 

ACM Transactions on Graphics (TOG) , 36(4):1–13, 2017. [20] H. Zhang, S. Starke, T. Komura, and J. Saito. Mode-adaptive neural networks for quadruped motion control. ACM Transactions on Graphics (ToG) , 37(4):1–11, 2018. [21] S. Starke, H. Zhang, T. Komura, and J. Saito. Neural state machine for character-scene inter-actions. ACM Transactions on Graphics , 38(6):178, 2019. [22] S. Starke, Y. Zhao, T. Komura, and K. Zaman. Local motion phases for learning multi-contact character movements. ACM Transactions on Graphics , 39(4), 2020. [23] I. Mason, S. Starke, and T. Komura. Real-time style modelling of human locomotion via feature-wise transformations and local motion phases. Proceedings of the ACM on Computer Graphics and Interactive Techniques , 5(1):1–18, 2022. [24] X. Zhang, Z. Chang, Q. Men, and H. P. Shum. Motion in-betweening for densely interacting characters. In Proceedings of the SIGGRAPH Asia 2025 Conference Papers , pages 1–11, 2025. [25] M. Dai, K. Fan, A. Rao, J. Wang, and B. Dai. Controllable text-to-motion generation via modular body-part phase control. arXiv preprint arXiv:2603.19795 , 2026. [26] J. Park, D. Cho, J. Lee, D. Shim, I. Jang, and H. J. Kim. Periodic skill discovery. Advances in Neural Information Processing Systems , 38:105404–105438, 2026. [27] M. Gleicher. Retargetting motion to new characters. In Proceedings of the 25th annual con-ference on Computer graphics and interactive techniques , pages 33–42, 1998. [28] K. Aberman, P. Li, D. Lischinski, O. Sorkine-Hornung, D. Cohen-Or, and B. Chen. Skeleton-aware networks for deep motion retargeting. ACM Transactions on Graphics (ToG) , 39(4): 62–1, 2020. [29] Z. Luo, J. Cao, K. Kitani, W. Xu, et al. Perpetual humanoid control for real-time simulated avatars. In Proceedings of the IEEE/CVF International Conference on Computer Vision , pages 10895–10904, 2023. [30] Z. Luo, J. Cao, J. Merel, A. Winkler, J. Huang, K. Kitani, and W. Xu. Universal humanoid motion representations for physics-based control. In International Conference on Learning Representations , volume 2024, pages 56766–56782, 2024. [31] L. Yang, X. Huang, Z. Wu, A. Kanazawa, P. Abbeel, C. Sferrazza, C. K. Liu, R. Duan, and G. Shi. Omniretarget: Interaction-preserving data generation for humanoid whole-body loco-manipulation and scene interaction. arXiv preprint arXiv:2509.26633 , 2025. [32] Y. Yan, E. V. Mascaro, and D. Lee. Imitationnet: Unsupervised human-to-robot motion retar-geting via shared latent space. In 2023 IEEE-RAS 22nd International Conference on Humanoid Robots (Humanoids) , pages 1–8. IEEE, 2023. 10 [33] X. Cheng, Y. Ji, J. Chen, R. Yang, G. Yang, and X. Wang. Expressive whole-body control for humanoid robots. arXiv preprint arXiv:2402.16796 , 2024. [34] Y. Yan and D. Lee. Learning a unified latent space for cross-embodiment robot control, 2026. URL https://arxiv.org/abs/2601.15419 .[35] R.-Z. Qiu, S. Yang, X. Cheng, C. Chawla, J. Li, T. He, G. Yan, D. J. Yoon, R. Hoque, L. Paulsen, G. Yang, J. Zhang, S. Yi, G. Shi, and X. Wang. Humanoid policy human policy, 2025. URL https://arxiv.org/abs/2503.13441 .[36] C. Wang, L. Fan, J. Sun, R. Zhang, L. Fei-Fei, D. Xu, Y. Zhu, and A. Anandkumar. Mimicplay: Long-horizon imitation learning by watching human play, 2023. URL https://arxiv.org/ abs/2302.12422 .[37] P. Dan, K. Kedia, A. Chao, E. W. Duan, M. A. Pace, W.-C. Ma, and S. Choudhury. X-sim: Cross-embodiment learning via real-to-sim-to-real. arXiv preprint arXiv:2505.07096 , 2025. [38] H. Kim, J. Kang, H. Kang, M. Cho, S. J. Kim, and Y. Lee. Uniskill: Imitating human videos via cross-embodiment skill representations. arXiv preprint arXiv:2505.08787 , 2025. [39] Y. Shao, X. Huang, B. Zhang, Q. Liao, Y. Gao, Y. Chi, Z. Li, S. Shao, and K. Sreenath. Langwbc: Language-directed humanoid whole-body control via end-to-end learning. arXiv preprint arXiv:2504.21738 , 2025. [40] J. Yue, Z. Wang, Y. Wang, W. Zeng, J. Wang, X. Xu, Y. Zhang, S. Zheng, Z. Ding, and Z. Lu. Rl from physical feedback: Aligning large motion models with humanoid control. arXiv preprint arXiv:2506.12769 , 2025. [41] Z. Li, C. Chi, Y. Wei, B. Zhu, Y. Peng, T. Huang, P. Wang, Z. Wang, S. Zhang, and C. Xu. From language to locomotion: Retargeting-free humanoid control via motion latent guidance. 

arXiv preprint arXiv:2510.14952 , 2025. 11 Appendix 

## A Implementation Details 

A.1 Body-Part Partition and Channel Allocation 

To systematically capture local periodic structures, we partition kinematic joints of each embodiment into five semantic parts: B = {LA , RA , TK , LL , RL }. We assign the phase channel allocation 

Kb based on the rhythmic complexity of each part during locomotion. Specifically, legs exhibit strong multi-phase oscillations requiring more capacity ( KLL = KRL = 4 ), arms show moderate complexity ( KLA = KRA = 3 ), and the trunk’s dominant modes are set to KTK = 2 .This allocation is shared across all embodiments, yielding a universal Mtotal = 16 channels and a 32-dimensional phase manifold P(τ ). Tab. I summarizes the joint distributions and kinematic dimensions for the human and four humanoid robots.                                                        

> Embodiment JDkJoints per part TK Components
> LA RA TK LL RL Human (SMPL-H) 22 66 44644pelvis, spine (3), neck, head G1 (Unitree) 32 96 77477pelvis, waist (2), torso H12 (Unitree) 28 84 77266pelvis, torso T1 (Booster) 24 72 44466trunk, head (2), waist BHL (Berkeley) 25 75 66166base only
> Phase channels ( Kb)33244Mtotal = 16

Table I: Body-part partition and channel allocation. We enforce a consistent five-part semantic structure and phase channel allocation ( Kb) across heterogeneous embodiments. J denotes total joints, and Dk is velocity dimension ( 3J). The 3D root linear velocity is concatenated to the trunk (TK) input. 

Embodiment Part Joint Indices & Description 

Human (SMPL-H) LA / RA [13, 16, 18, 20] / [14, 17, 19, 21]: collar, shoulder, elbow, wrist TK [0, 3, 6, 9, 12, 15]: pelvis, spine 1–3, neck, head LL / RL [1, 4, 7, 10] / [2, 5, 8, 11]: hip, knee, ankle, foot G1 (Unitree) LA / RA [18–24] / [25–31]: shoulder (3), elbow, wrist (3) TK [0, 15–17]: pelvis, waist (2), torso LL / RL [1–7] / [8–14]: hip (3), knee, ankle (2), toe H12 (Unitree) LA / RA [14–20] / [21–27]: shoulder (3), elbow, wrist (3) TK [0, 13]: pelvis, torso LL / RL [1–6] / [7–12]: hip (3), knee, ankle (2) T1 (Booster) LA / RA [3–6] / [7–10]: arm joints 1–3, hand TK [0–2, 11]: trunk, head 1–2, waist LL / RL [12–17] / [18–23]: hip (3), shank, ankle (cross), foot BHL (Berkeley) LA / RA [1–6] / [7–12]: shoulder (3), elbow (2), hand TK [0]: base only LL / RL [13–18] / [19–24]: hip (3), knee, ankle (2) 

Table II: Explicit joint index mappings. Joints are assigned to the five semantic parts following each embodiment’s specific URDF hierarchy. Left/Right symmetric parts are grouped for brevity, and the numbers in parentheses indicate the degrees of freedom (e.g., pitch, roll, yaw) for that joint group. iA.2 Bidirectional FiLM Coupling Architecture 

We provide the full architectural details of the bidirectional FiLM coupling described in Sec. 4.1.2. Both directions pass through a learned manifold encoder ( Eψ for phase-to-pose, Eϕ for pose-to-phase) that aligns the source representation to the temporal granularity of the target branch. 

Phase-to-Pose Modulation. The phase-to-pose direction provides the pose decoder with temporal context: “where in the action the body currently is”. The manifold encoder Eψ compresses the per-frame phase manifold P ∈ RT ×32 into a token-aligned context Eψ (P) ∈ RLpose ×dg with dg = 128 ,implemented as three 1D convolutions followed by adaptive pooling to Lpose = 8 tokens. A FiLM head produces per-token modulation parameters (γ, β ) ∈ RLpose ×dpose that scale and shift the pose tokens: 

zmod pose = (1 + αγ ) ⊙ zpose + αβ. (9) 

Pose-to-Phase Modulation. Symmetrically, the pose-to-phase direction provides each per-part de-coder with postural context: “what baseline configuration the motion rests on”. The manifold en-coder Eϕ expands the pose tokens zpose ∈ RLpose ×dpose into per-frame context Eϕ(zpose ) ∈ RT ×dg, pose 

with dg, pose = 64 , implemented as two 1D convolutions followed by linear interpolation from 

Lpose = 8 tokens to T = 121 frames. A per-part FiLM head generates per-frame modulation parameters (γb(τ ), β b(τ )) ∈ RKb that modulate the reconstructed sinusoidal signal ˆvsig  

> b

(τ ) =P 

> k

Ab,k cos ϕb,k (τ ) + Bb,k before it enters the per-part decoder Db:

ˆvsig,mod  

> b

(τ ) =  1 + αγ b(τ ) ⊙ ˆvsig  

> b

(τ ) + αβ b(τ ). (10) 

Modulation Strength. The scalar α is learnable in both directions and initialized to 0.05 . This near-zero start ensures that the modulation begins close to identity, providing the same training stability while allowing the model to gradually scale up modulation as training progresses. 

A.3 Periodic Module Details 

We detail the per-part periodic encoder Φb, the FFT-based parameter extraction, and the handling of root velocities. 

Per-Part Encoder and Decoder. Each Φb is a lightweight 1D convolutional encoder of two layers with LayerNorm over the time dimension and ELU activations. The latent map Φb(vb) ∈ RKb×T

feeds the FFT bottleneck. The per-part decoder Db mirrors Φb with the same width and depth, taking the modulated sinusoidal signal ˆvsig,mod  

> b

(τ ) and producing the reconstructed velocity. For the trunk, the decoder output is sliced into the joint-velocity ( ˆvjoint TK ) and the root linear velocity portion ( ˆvroot lin ). 

FFT Parameter Extraction. Given the per-part latent Φb(vb), the periodic parameters are extracted by applying a differentiable real-valued FFT along the time axis. Let {cj } denote the FFT coeffi-cients of latent channel k. The amplitude, frequency, and offset are computed as 

Ab,k =

s 2

> T

X

> j≥1

|cj |2, Fb,k =

P 

> j≥1

fj |cj |2

P 

> j≥1

|cj |2 , Bb,k = 1 

> T

c0, (11) where fj is the FFT bin frequency. The phase shift Sb,k is predicted by a separate fully-connected head followed by arctan2 , producing values in (−π, π ].

Trunk Input with Root Linear Velocity. The root linear velocity vroot lin ∈ RT ×3 exhibits clear periodicity during locomotion (vertical oscillation and forward-backward acceleration), and the phase branch should absorb it. We concatenate it to the trunk joint velocities: vTK (τ ) = [vjoint TK (τ ); vroot lin (τ )] . The root yaw rate is omitted from the phase branch, since the pose branch already absorbs angular changes through the temporal evolution of Rjoint .

Pelvis vx, v z Soft Mask. Under the root-local frame, the pelvis horizontal velocity components 

vx, v z are identically zero by definition. Treating them with a hard zero mask in Lphase caused training drift of approximately ±0.8 on neighboring channels. We instead apply a soft mask with ii weight 0.1, which anchors these channels to zero while keeping a small but nonzero gradient signal. This stabilizes the trunk reconstruction without distorting the manifold. 

A.4 Cross-Embodiment Pipeline Details 

We provide details of the encoder and decoder adapters and the robot pose branch. 

Per-Robot Input Adapter Ekr . The input adapter maps the robot velocity dimension Dk to the human dimension 66 via a single global linear layer, Ekr = Linear (Dk → 66) . We considered a per-part variant with five separate linear layers, one per body part, but the two options produced near-identical retrieval and reconstruction quality. We adopt the global linear variant since it requires no hardcoded joint-to-part mapping and allows cross-joint information flow to be learned end-to-end. 

Forward Pipeline for Robots. Given a robot velocity input, the pipeline proceeds as follows. (1) Apply the per-robot input adapter: ˜v(66)  

> Rk

= Ekr (vjoint  

> Rk

). (2) Split the 66-D output into the five-part dictionary using the same partition as the human. (3) Concatenate the robot’s root linear velocity 

vroot lin ,k to the trunk input. (4) Apply the frozen weight-shared encoder EX ≡ Eh to extract per-part periodic parameters. (5) Construct PRk (τ ) ∈ RT ×32 via Eq. (3). 

Decoder Split and Mirror Initialization. The human phase decoder Dh remains frozen and is used only on the human path. Robot paths share a unified decoder DX that is initialized as a weight-copy of Dh and fine-tuned on robot data. This split provides a gradient safety net: robot reconstruction loss flows only through DX and never disturbs Dh. The mirror initialization gives DX a strong starting point, since robot motion is itself a retargeted humanoid signal and the human velocity decoder serves as a valid prior. We also tested freezing DX at its mirror copy; fine-tuning improved retrieval mAP and reconstruction by approximately 1% on average and was adopted as the default. 

Per-Robot Pose Branch. The pose branch is instantiated separately for each embodiment, since posture distributions and joint conventions vary across morphologies. Tab. III summarizes which modules are shared with the human oracle and which are trainable per-embodiment. The phase-side modules ( Eψ and the phase-to-pose FiLM head) are shared and frozen, consistent with anchoring the manifold to the human reference. The pose-side modules ( Eϕ and the pose-to-phase FiLM head) are per-embodiment trainable, since their input zpose comes from an embodiment-specific encoder with a different distribution.                

> Module Human Robot k
> Pose encoder + decoder frozen per-embodiment, trainable
> Eϕ(Pose →Phase manifold enc.) frozen per-embodiment, trainable Pose →Phase FiLM head frozen per-embodiment, trainable
> Eψ(Phase →Pose manifold enc.) shared, frozen shared, frozen Phase →Pose FiLM head shared, frozen shared, frozen

Table III: Pose branch module sharing across embodiments. Phase-side conditioning modules are shared with the frozen human oracle, while pose-side conditioning modules are trainable per-embodiment. 

A.5 Alignment Objective Details Alignment Head Architecture. The projection π takes the per-frame concatenated feature f (τ ) = [P(τ ); zpool pose ] ∈ R160 and produces a unit-sphere embedding. Its structure is Linear (160 → 64) →

GELU → Linear (64 → 64) → ℓ2. Per-frame normalization yields the trajectory z(τ ) ∈ S 63 , and a subsequent time-pool with another ℓ2 normalization yields the clip-level descriptor zpool ∈ S 63 .The trajectory z(τ ) feeds Lspeed and Lshape , while zpool feeds Lpair .

Pose-Half Near-Zero Initialization. The first linear layer of π has weight matrix W ∈ R64 ×160 .We initialize the first 32 columns (the phase input half) with standard Kaiming initialization and the remaining 128 columns (the pose input half) with a near-zero scale of 10 −3. At the start of training, iii π thus behaves close to a phase-only projection, and the pose contribution grows gradually through gradient updates. This warm-onset prevents pose noise from disrupting alignment early and keeps the pose contribution at an auxiliary level by the end of training. 

Hierarchical Pair Loss. We apply the LAMP soft target at two granularities. Let ℓW 

> →

denote the 

B × B window-level logits over the human-to-robot or robot-to-human direction, and T soft W the corresponding LAMP-induced soft target from per-window LAMP features. The window-level loss is 

L(k) 

> pair,W

= 12

X

> →

CE  ℓW

> →

, (1 − λmix )T hard + λmix T soft W

. (12) For the clip-pool head, we average z over windows sharing the same clip, normalize, and form a 

K × K logit matrix where K is the number of unique clips in the batch. The corresponding LAMP soft target T soft C uses clip-level LAMP features: 

L(k) 

> pair,C

= 12

X

> →

CE  ℓC

> →

, T soft C

. (13) The clip-pool head uses a pure soft target since same-clip windows are already merged into a single anchor through pooling, removing the gradient-dilution effect of broadcasting soft labels across redundant windows. The final pair loss combines both granularities: 

L(k) 

> pair

= L(k) 

> pair,W

+ λCL(k)

> pair,C

. (14) 

Trajectory Consistency Terms. The two auxiliary geometric penalties on z(τ ) ∈ S 63 are 

L(k) 

> speed

= 1  

> T−1
> T−1

X 

> τ=1

∥zH (τ +1) −zH (τ )∥ − ∥ zRk (τ +1) −zRk (τ )∥ , (15) 

L(k) 

> shape

= 2

> TT / 2

X 

> τ=1

∥zH (τ +T / 2) −zH (τ )∥ − ∥ zRk (τ +T / 2) −zRk (τ )∥ . (16) 

Lspeed matches per-frame step size on the sphere, an implicit frequency synchronization. Lshape 

matches half-period distance, an implicit amplitude synchronization. Both operate on the post-projection unit sphere, making them scale-invariant by construction. 

LAMP Teacher. LAMP [16] is a pretrained motion-language prior trained on human motion. Its 512-D embedding serves as a fixed teacher. We use two granularities of LAMP features: clip-level embeddings for the clip-pool head, and stride-20 window-level embeddings for the window head. Both are precomputed on the human motion data and applied symmetrically as soft targets for human-to-robot and robot-to-human alignment in each batch. 

A.6 Training Procedure Reconstruction Loss Terms. The per-part velocity reconstruction loss applies a weighted MSE over the five body parts: 

L(e) 

> phase

= X

> b∈B

wb ∥ˆv(e) 

> b

− v(e) 

> b

∥22, (17) where the pelvis vx, v z channels carry a soft weight w = 0 .1 as described in Sec. A.3. The pose reconstruction loss combines a geodesic distance on 6D continuous rotations with a root position MSE: 

L(e) 

> pose

= geodesic( ˆRjoint ,(e), Rjoint ,(e)) + ∥ ˆXroot ,(e) − Xroot ,(e)∥22. (18) The forward kinematics consistency loss links the two branches by enforcing agreement on global joint positions: 

L(H) 

> FK

= FK( ˆRjoint , ˆXroot ) − FK( Rjoint , Xroot ) 22. (19) The FK loss is applied only on the human path, since we do not maintain a differentiable kinematic chain for each robot in this work. iv Group Item Value 

Window T , fps 121, 60 Phase Kb (LA, RA, TK, LL, RL) (3, 3, 2, 4, 4), Mtotal = 16 

Pose Lpose , dpose , dR, droot 8, 128, 96, 32 FiLM dg , dg, pose , α init 128, 64, 0.05 Alignment π input dim, dalign 160 (= 32 + 128), 64 pose-half init scale 10 −3

τh, τs 0.1, 0.07 

λmix , λC 0.3, 0.3 

λpair , λspeed , λshape 1.0, 0.5, 0.5 Loss weights λphase , λpose , λFK 5.0, 5.0, 1.0 pelvis vx, v z soft mask 0.1 Optimization optimizer, lr, weight decay AdamW, 2 × 10 −4, 10 −4

batch size, grad clip 512, 5.0 epochs (each stage) 30 FK warmup, alignment warmup 3, 5 epochs 

Table IV: Hyperparameter configuration for both training stages. 

H→R Clip-level R→H Clip-level R→R Clip-level 

Method R@1 R@5 R@10 mAP R@1 R@5 R@10 mAP R@1 R@5 R@10 mAP MLP 99.6 100.0 100.0 99.8 99.8 100.0 100.0 99.9 99.0 99.8 100.0 99.4 

SONIC 97.8 99.8 99.8 98.7 97.0 99.8 99.8 98.3 97.2 99.5 99.5 98.2 Only Phase 93.8 98.1 98.8 95.7 94.2 98.5 99.0 96.2 88.1 96.0 98.0 91.4 W.O LAMP 95.8 99.1 99.8 97.2 95.5 99.0 99.8 97.0 91.4 97.8 99.0 94.2 W Pos 97.5 99.9 100.0 98.5 98.8 99.8 100.0 99.3 93.5 99.3 99.7 96.0 

W Zpos (Ours) 99.1 99.8 99.8 99.4 99.5 99.8 99.8 99.6 98.1 99.4 99.7 98.6 

Table V: Cross-embodiment clip-level retrieval on the DPAE val set (2,472 clips). H→R: human query → robot gallery (avg. over G1/H1.2/T1/BHL). R→H: robot query → human gallery (avg. over G1/H1.2/T1/BHL). R→R: robot ↔ robot (avg. over  42

=6 pairs, both directions). All values in %. Bold : best per column. Underline: second best. 

Hyperparameters. Tab. IV lists the full hyperparameter configuration. All experiments share these values across the human and four robot embodiments. 

Parameter Accounting. At Stage 2, the human oracle contributes approximately 528K frozen pa-rameters, including the phase encoder body Eh (shared as EX on the robot path), the manifold encoder Eψ , the phase-to-pose FiLM head, the phase decoder Dh, the human pose encoder and de-coder, and the human pose-to-phase modules. The trainable budget is approximately 1.46M param-eters, consisting of the shared phase decoder DX ( 51K, mirror-initialized from Dh), the alignment head π ( 10K), per-robot input and output adapters Ekr , D kr ( 11K each), per-robot pose encoder and decoder ( 225K each), per-robot Eϕ ( 43K each), and per-robot pose-to-phase FiLM head ( 87K each), summed over the four robot embodiments. 

## B Additional Experiments 

B.1 Ablation 

We isolate each design choice through clip-level retrieval on Stage 2 (Sec. B.1). Clip-level matching only requires preserving coarse semantic identity, not fine temporal structure, making it an easy task: all methods exceed 91% mAP and gaps between variants are small. We read this as a sanity check that each variant retains clip-level semantics, while our true advantage appears in exact-match retrieval (Sec. 5.1.1), addressing the limitation in Sec. 6. vOnly Phase (phase manifold alone) already reaches 91 –96% mAP, confirming the periodic prior captures clip identity on its own; adding LaMP guidance ( W.O LAMP →default) and root position (W Pos ) each gives a small, consistent gain, largest on R →R. Our full model W Zpos is essen-tially tied with the best configuration everywhere. The MLP and VQ baselines lead at clip level, as expected: an unstructured objective directly optimizes coarse semantic identity, whereas our embed-ding additionally encodes when each chunk occurs, which is unnecessary here but drives our large margin in exact-match. Adding temporal structure thus costs almost nothing on this easy task while dominating the fine-grained one. 

B.2 Effect of the LAMP Mixing Ratio 

The hierarchical pair loss (Eq. (12)) mixes a hard-target InfoNCE term with a LAMP-induced soft target via λmix : at λmix =0 it is pure InfoNCE, at λmix =1 it relies entirely on LAMP semantic sim-ilarity. We sweep λmix ∈ { 0.0, 0.3, 0.7, 1.0} and report clip-level and exact-match mAP across the three directions (Fig. I). Both granularities peak at λmix =0 .3, our default. A moderate semantic signal helps, most visibly on the unsupervised R →R direction where it strengthens transitive alignment through the human anchor. But beyond 0.3 performance degrades, collapsing at λmix =1 .0: with the hard target removed, the model loses the instance-level correspondence needed for fine-grained matching, since LAMP similarity alone is too coarse to separate chunks within a clip. LAMP thus works best as auxiliary guidance, not a standalone target. 

Figure I: Effect of the LAMP mixing ratio λmix . Clip-level (left) and exact-match (right) mAP over λmix ∈ { 0.0, 0.3, 0.7, 1.0} for all three directions. Both peak at 0.3 (our default) and collapse at 1.0 when the hard target is removed. 

B.3 Downstream Tasks 

We probe the frozen embedding’s utility in three settings: single-embodiment next-frame prediction (imitation), cross-embodiment teleoperation, and locomotion RL with a phase-shaped reward. In both offline tasks the policy head is a 3-layer MLP ( 256 → 256 →output) trained for 50 epochs (Adam, lr 10 −4, cosine schedule, batch 512). A no-embedding baseline (A) receiving only raw proprioceptive state is included for reference. 

B.4 RL Locomotion Details B.4.1 Reference Embedding Preparation 

Before RL training, every AMASS reference clip is encoded offline into per-frame reference embed-dings zref by each model’s frozen human encoder, yielding a 64D sequence for PHASOR, 10D for VQ, and 80D for MLP. Each variant’s reward uses its own pre-encoded zref , so the human encoder need not run during training. vi B.4.2 Environment Configuration 

• Simulator: IsaacLab with GPU physics. • Simulation timestep 1/120 s, control decimation 2, giving a policy timestep of 1/60 s. • 8,192 parallel environments. • Episode length: up to 20 s ( 1,200 policy steps). • Each episode resets to a standing pose (no reference-state initialization). 

B.4.3 Observation 

The policy observation is a 10-step history of proprioceptive state, concatenating per step the joint positions (23), joint velocities (23), base angular velocity (3), base roll/pitch/yaw (3), and previous action (23), for a total of 750 dimensions. This is augmented with the frozen robot embedding 

zalign r , whose dimension depends on the encoder: 

Variant zalign r dim Total obs dim PHASOR 64 814 MLP 80 830 VQ 10 760 

The robot embedding is computed online: at each step the latest joint velocities are appended to a 121-frame sliding window, and once the window is full the frozen robot encoder produces zalign r .

B.4.4 Reference Progression 

At each reset a reference clip is sampled at random, and its frame index advances by one per policy step (wrapping at the clip end). The corresponding reference embedding zref cur is read from the precomputed sequence. The reference is used only to compute the reward and is never part of the observation. 

B.4.5 Reward Design (15 Terms) Survival (always active): 

# Term Weight 1 Termination penalty (torso height < 0.3 m) −200 

2 Alive bonus ( +0 .15 per step) ×1

3 Flat orientation: −(g2 

> b,x

+ g2 

> b,y

) −1.0

4 Base height: −(zpelvis − 0.78) 2 −10 .0

5 Vertical velocity: −v2 

> z

−2.0

6 Roll/pitch angular velocity −0.05 

Locomotion (active from iteration 500): 

# Term Weight 7 Velocity tracking: exp( −(vrobot − vref )2/0.25) λvel 

8 Feet air-time (bipedal alternation bonus) +2 .0

9 Feet slide penalty −0.1

10 Hip joint deviation from default −0.5

11 Arm joint deviation from default −0.1

12 Torso joint deviation from default −0.1

13 Ankle soft-limit violation −1.0

14 Joint acceleration (hip, knee) −1.25 ×10 −7

15 Joint torque (hip, knee, ankle) −1.5×10 −7

16 Action rate: −(at − at−1)2 −0.001 

Phase (active from iteration 1000): 

vii # Term Weight 17 Phase similarity: (cos( zalign r , z ref cur ) + 1) /2 λphase 

Non-termination terms are clamped to be non-negative before the termination penalty is added, so the agent cannot reduce its loss by ending an episode early; the termination penalty therefore always applies. 

B.4.6 PPO Configuration 

• 5 PPO epochs, 4 mini-batches per iteration; 2,500 iterations total. • Policy network: MLP [512 → 256 → 128] with ELU, observation normalization, and Gaussian action noise σ = 0 .3.• Curriculum weights updated every 50 iterations. 

B.4.7 Curriculum Schedule 

Iteration λvel λphase Stage 0–499 0 0 Survival only (stand) 500–999 1 0 + velocity (walk) 1000–1999 1 0 → 1 + phase ramp (shape gait) 2000–2500 1 1 Full reward 

viii